playwright>=1.44
python-dotenv>=1.0
requests>=2.31
cryptography>=42.0
google-api-python-client>=2.0   # sheets_sync
google-auth>=2.0                # sheets_sync (service account)
psycopg2-binary>=2.9            # backend (Supabase/Postgres)
fastapi>=0.110                  # auth_broker (Church-login API)
uvicorn>=0.29                   # auth_broker (ASGI server)
httpx>=0.27                     # auth_broker tests (FastAPI TestClient)
webauthn>=2.0                   # auth_broker passwordless passkey login (server-side WebAuthn)
Pillow>=10.0                    # member-photo pipeline (downsize avatars)
tzdata>=2024.1                  # ET schedule gate (zoneinfo needs the tz DB on minimal images/Windows)
