# Deprecated — Flutter app (frozen 2026-06-08)

This is the original **Flutter** viewer (was `apps/viewer`) — one codebase that built web + iOS +
macOS + Android. It is **deprecated and frozen: do not edit it.**

Replaced by three maintained, side-by-side codebases (updated together — see root `CLAUDE.md`):

| Surface | Where |
|---|---|
| **Web** | React — `apps/web` (deployed to Cloudflare Pages) |
| **iOS** | native Swift — `native/ios` |
| **Android** | native Kotlin — `native/android` |

Kept for reference / rollback only.
