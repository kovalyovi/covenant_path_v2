# NOTE: gradle-wrapper.jar + gradlew/gradlew.bat ARE committed (below) so CI can build without a
# pre-installed Gradle — standard practice for a tracked Android project.
/.gradle
/.kotlin/
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
