# Testing — lanes, rules, and the scenario catalog

This is the **source of truth for what must be tested and when** (companion to CLAUDE.md rule 4).
The 2026-06-10 enroll outage (a `NameError` that broke **every** consented enroll for 8 hours and
shipped because no test drove the eval past the fast paths) is the reason this file exists: every
user-visible behavior gets a scenario here, every scenario gets a test, and CI runs the right
suites whenever their code is touched.

## The rule

1. **Touch a feature → run its lane locally → CI re-runs it on push** (`.github/workflows/tests.yml`
   path-filters do the mapping; nightly runs everything including the live lanes).
2. **A user-facing change MUST add or extend a scenario row below** (and its test) in the same
   commit. A bug fix MUST add the regression scenario that would have caught it.
3. **Public repo invariants**: committed fixtures are 100% fictional (unit 999001+, fake names);
   no secrets in workflows readable from forks (no `pull_request` triggers on secret-bearing
   jobs); `CP_TEST_MODE` is never set on Render/CI-sync — `tools/test_suite.py::
   test_hosts_test_mode_guard` proves the host override is dead without it.

## Lanes

| Lane | What it is | Command | Secrets | CI trigger |
|---|---|---|---|---|
| Backend offline | Pure-logic + crypto + broker unit/regression suites | `python tools/test_suite.py` · `python -m backend.test_broker` | none | push (backend paths) + nightly |
| Lint | `ruff` F821 (undefined names — the enroll-outage class) | `python -m ruff check --select F821 backend lcr_client covenant_path sheets_sync scripts tests` | none | push (backend) + nightly |
| **Backend e2e (mock lane)** | The **real broker + real lcr_client** against the fixture **mock LCR/Okta** (`tests/mock_lcr`) + **Supabase stub** (`tests/supabase_stub`), via the `CP_TEST_MODE=1` host override | `python -m pytest tests/e2e -q` | none | push (backend) + nightly |
| Web unit | vitest + typecheck + build | `npm run typecheck && npm run test && npm run build` (apps/web) | none | push (web) + nightly |
| **Web e2e (UI lane)** | Playwright against the real app with broker/Supabase mocked at the network edge | `npm run e2e` (apps/web) | none | push (web) + nightly |
| Live smoke (prod) | The existing live suites against production Supabase | `python -m backend.test_rls` · `test_power_users` · `test_admins` · `test_login_audit` · `test_reconcile` · `test_calling_overrides` | prod secrets | nightly + dispatch only |
| **RLS matrix (test project)** | Migrations + seeded personas + the role×table visibility matrix against the **dedicated test Supabase project** | `python -m tests.seed && python -m pytest tests/test_rls_matrix.py` | `CP_TEST_SUPABASE_*` | nightly + dispatch (skips until the project exists) |
| **Fullstack e2e (LIVE)** | Playwright `fullstack` project: real browser → real broker → mock LCR → the TEST project (RLS live). The config boots the whole stack itself (`tests/fullstack_stack.py` + vite) | `E2E_FULLSTACK=1 npx playwright test --project=fullstack` (apps/web; needs `.env` CP_TEST_* + seeded project) | `CP_TEST_SUPABASE_*` | local / dispatch |
| Native | iOS/Android build + manual AVD pass | CI `build-native-*.yml`; AVD per `native/PARITY.md` | none | push (native paths) |

## Test environment

- **Mock hosts**: `lcr_client/hosts.py` — `CP_TEST_MODE=1` + `CP_TEST_LCR_BASE`/`CP_TEST_IDENTITY_BASE`
  point ALL Church-host traffic at `tests/mock_lcr`. Without `CP_TEST_MODE` the overrides are
  ignored (guard test). Never set `CP_TEST_MODE` outside tests.
- **Mock personas** (username / password → behavior): `president.complete`/`pw-president`
  (no-MFA stake president, full access), `wardleader.partial`/`pw-ward` (partial coverage),
  `member.nocalling`/`pw-member` (valid login, no calling — the gate case), `member.mfa`/`pw-mfa`
  (MFA shape A, code `123456`), `member.mfab`/`pw-mfab` (shape B), `member.mfaw`/`pw-mfaw`
  (shape A + a webauthn security key on the menu; wrong code answers the PROD field-level 401 —
  the 2026-06-11 incident), `user.locked` (locked account),
  anything else → "Authentication failed". Failure injection: `POST /__test/control`
  (`lcr_5xx` / `identity_timeout` / `sso_reject` / `healthy`).
- **Dedicated TEST Supabase project** (Phase B, owner action): create a SECOND free Supabase
  project; set `CP_TEST_SUPABASE_URL` / `CP_TEST_SUPABASE_ANON_KEY` /
  `CP_TEST_SUPABASE_SERVICE_ROLE_KEY` / `CP_TEST_SUPABASE_DB_URL` in `.env` (local) and GitHub
  secrets (CI). `tests/seed.py` refuses to run when these are unset **or equal the prod values**.
- **HAR-derived schemas**: `tools/output/har_schema_report.md` + `har_schemas.json` (gitignored,
  regenerable) document the REAL LCR response shapes the mock fixtures mirror — including real
  failure bodies (`auth/me` 502 `text/plain`, `one-work/*` 500 empty body, Okta `sessions/me`
  404-before-login) and the no-calling persona. Never copy real values into fixtures.

## Scenario catalog

Status: ✅ covered (lane · test) · 🔶 Phase B (needs test project / fullstack lane) · ⬜ TODO.
**Add rows here as features land — a PR that changes behavior without touching this table is
incomplete.**

### A. Login & messages

| # | Scenario | Status |
|---|---|---|
| A1 | Happy password login (no MFA) → session minted, authorized, audit row | ✅ e2e-mock · `tests/e2e` |
| A2 | Wrong username/password → friendly "username or password is incorrect", 401; audit keeps RAW cause + `okta:bad_credentials` | ✅ offline `test_broker` + e2e-mock + Playwright `login-messages` |
| A3 | Locked account → unlock instructions message | ✅ offline `test_broker::test_friendly_okta_errors` |
| A4 | LCR outage after password OK (502) → honest "LCR itself appears to be down" 503; audit `identity:lcr_5xx` + root cause | ✅ offline (classifier) + e2e-mock (`lcr_5xx` scenario) |
| A5 | LCR slow/timeout → "slow or briefly down" message; retries within budget | ✅ offline `test_broker` (retry tests, 6802d6e) |
| A6 | SSO rejected → "try signing in at lcr.churchofjesuschrist.org" | ✅ offline (classifier) |
| A7 | Outage suffix "failing for everyone since HH:MM" after 2+ distinct users; `/health` `lcr: degraded` | ✅ offline `test_broker` |
| A8 | No-calling member blocked IN the response (first login sync gate; cached fast block on repeat) | ✅ offline `test_broker` + e2e-mock |
| A9 | No-calling block message = `kNoAccessMessage`, stays on login, no session | ✅ Playwright `login-messages` |
| A10 | MFA shape A: factor list → select → code → success; wrong code → friendly retry, state preserved | ✅ e2e-mock + Playwright `login-mfa` |
| A11 | MFA shape B (auto-sent single factor) | ✅ e2e-mock |
| A18 | **2026-06-11 MFA hardening** — unsupported factors (webauthn/push) FILTERED from the menu; all-unsupported → actionable "add a method at churchofjesuschrist.org"; answer body uses the challenge's declared field (`totp` vs `passcode`); field-level "Invalid code" 401 (no top-level messages) → friendly retry, NEVER raw IDX JSON; pending state refreshed from the failure payload (retry-safe); code normalized (spaces/hyphens) | ✅ offline `test_broker` (regression suite, fails pre-fix) + e2e-mock `test_mfa_webauthn_filtered_and_field_level_invalid_code` |
| A19 | MFA audit completeness: `mfa_pending` row on hand-off (offered + dropped factor types), `mfa_select_failed` audited, `mfa_failed` carries username + factor type in phase (`okta:mfa_bad_code:phone_number`) | ✅ offline `test_broker::test_mfa_pending_and_select_failures_audited` + e2e-mock |
| A20 | MFA UI hygiene (all 3 surfaces): code input clears on factor switch + failed verify; digits-only, Verify gated on ≥6; "Send a new code" with 30s cooldown; "Start over" escape; mode switch + passkey button hidden mid-MFA | ✅ Playwright `login-mfa` + `reauth-dialog` (web) · 🔶 native via CI builds + AVD spot-check (PARITY.md 2026-06-11) |
| A22 | MFA code-SOURCE naming (all 3 surfaces + broker): code-step prompt names where the code comes from (texted to Okta's masked number / authenticator app / newest email) + "don't use an authenticator code here" warning + "no text? number may be out of date" hint; rejected-code error gives factor-specific guidance (`_BAD_CODE_HINTS`) — the wrong-source code is the proven 2026-06-11 trap | ✅ vitest `mfaCopy.test.ts` + offline `test_broker::test_mfa_bad_code_factor_guidance` + e2e-mock (phone hint in detail) + Playwright `login-mfa` · 🔶 native mirrors (`MfaCopy.swift`/`MfaCopy.kt`) via CI + AVD |
| A12 | Cached-identity fast lane: repeat login ~zero LCR (`timing.lane == "cached"`) | ✅ e2e-mock (hit counters) |
| A13 | Broker cold start → "Waking up…" status, login still completes | ✅ Playwright `login-messages` + existing `broker.test.ts` |
| A14 | Email-OTP login (direct + relay; cooldown) | ✅ offline (`test_email_relay_validation`) · 🔶 fullstack browser pass |
| A15 | Passkey register + passwordless login ceremony (CDP virtual authenticator; attestation row in the real `webauthn_credentials`; RLS session minted) | ✅ fullstack `apps/web/e2e/fullstack/passkey.spec.ts` (caught the `_rest` duplicate-headers 500 that broke BOTH prod ceremonies, fixed 2026-06-11) |
| A16 | Email/auth_id RLS spoof probe: two auth users sharing an email see only their own scope | 🔶 `tests/test_rls_matrix.py` |
| A17 | Unauthorized stranger from a NEW stake (valid Church account): LCR up → calling gate or empty-scope; LCR down → outage message (never data) | ✅ e2e-mock (A4+A8) · 🔶 matrix (zero-row visibility) |
| A23 | **Rate limiting (BACKEND-01)** — per-IP + per-identifier sliding-window caps on `/auth/password`, `/auth/mfa/*`, `/auth/otp/*`, `/auth/email/*`, `/webauthn/login/*` (429 over limit; env-tunable `RL_*`; auto-disabled under `CP_TEST_MODE`/pytest so test suites don't false-429); a pending MFA/OTP login is DROPPED after `MFA_MAX_FAILS` (default 6) wrong codes so a captured `login_id` can't brute-force the 6-digit code | ✅ offline `test_broker::test_ratelimit_check_caps_attempts` + `::test_mfa_lockout_after_max_fails` |
| A24 | **Broker output/injection hardening (PR C)** — report/contact/admin-invite/enroll emails HTML-escape LCR/user strings (BACKEND-02); WebAuthn `credential_id` validated base64url before the PostgREST filter (BACKEND-03); passkey login requires user-verification + rejects sign-count regression (BACKEND-09); Google OAuth `state` is single-use (BACKEND-05); Drive-callback + GitHub errors are generic to clients (BACKEND-08); a data route rejects an unauthenticated call (BACKEND-04); client error telemetry scrubs PII before `/log` on all 3 surfaces (CLIENT-04) | ✅ offline `test_broker` (`test_report_html_escapes_member_data`, `test_safe_cred_id_rejects_filter_metachars`, `test_user_routes_require_auth`) + `tools/test_suite.py::test_google_oauth_state` (single-use) + web `errorReporter.test.ts` · 🔶 passkey UV via fullstack `passkey.spec.ts`; native error-scrub mirrors via CI |
| A25 | **Web hardening (PR E)** — `Icon` renders `title` via the React-escaped `aria-label`, never interpolated into raw SVG HTML (CLIENT-03); broker CORS is pinned to this project's Cloudflare Pages site so a stranger's `*.pages.dev` deploy is denied (BACKEND-07) | ✅ web `icon.test.tsx` + offline `test_broker::test_cors` (denies `evil.pages.dev`, fails pre-fix) |
| A26 | **Android secure session storage (CLIENT-01/02)** — the Supabase session (access + refresh token) is persisted in Keystore-backed `EncryptedSharedPreferences` (not plaintext `SharedPreferences`) with `allowBackup="false"`; wiring is defensive (init failure → default manager, no startup crash); biometric app-lock documented as defense-in-depth | 🔶 native CI build + AVD: sign in → kill/relaunch (session restores) → `shared_prefs/cp_auth.xml` is ciphertext; confirm supabase-kt 3.1.1 SessionManager compiles. iOS uses the Keychain already |
| A27 | **Email-OTP field autofill (Safari/iPhone)** — the email-code fields render in their OWN `<form>` (Safari classifies password autofill per form element): sharing one form with the Church username+password fields made iPhone QuickType keep offering the saved LCR username on the email field even with `autocomplete="email"` (2026-06-12); the email form must be a distinct element containing nothing password/username-shaped | ✅ Playwright `login-messages` (fails pre-fix: both modes shared one form node) · native N/A (iOS already `.textContentType(.emailAddress)`; Android `KeyboardType.Email`) |
| A28 | **OTP identifier = Church USERNAME, not email (the enumeration-prevention phantom)** — the Church Okta org matches only the username: an email/unknown identifier gets Okta's phantom flow (menu offered, select accepted, "code sent", **no email ever sent**, every code "invalid" — wire-indistinguishable by design, probe-proven 2026-06-12; stranded Ken Packer's stake onboarding + the operator). Fix: (a) broker resolves a typed email → cached Church username via `church_identities` before identifying (returning users can keep typing their email); (b) unresolved email identifiers get `identifier_hint` in code_sent + username guidance appended to a rejected code; (c) the OTP lane rewrites the MFA-menu "choose a verification method" dead-end (challenge-less pending after a failed verify) to "Send a new code" guidance; (d) all 3 surfaces relabel the field "Church username" (username autofill semantics) with pre-send + post-send warnings on email-shaped input; (e) successful sends are audited (`otp_code_sent`); (f) Okta **silently pauses email delivery after a send burst** (observed live: 5 delivered in 36 min, then 40+ min of accepted-but-unsent selects) — the broker counts sends per identifier and the 4th+ within an hour returns `throttle_hint`, which all surfaces show on the code screen | ✅ offline `test_broker::test_otp_email_identifier_guidance` + `::test_otp_start_resolves_cached_email` + `::test_otp_send_burst_throttle_hint` (fail pre-fix) + Playwright `reauth-dialog` (A28 + throttle tests) + vitest `mfaCopy.test.ts` · 🔶 native mirrors (`MfaCopy.swift`/`MfaCopy.kt`, Reauth sheets) via CI + AVD |
| A29 | **MFA continuation in the passwordless lane (WITH and WITHOUT MFA)** — an MFA-enabled account's emailed code is ACCEPTED and Okta then demands a DISTINCT factor type (for Church accounts: the password — phone/TOTP share email's possession type, webauthn can't ride the broker); pre-fix the broker classified the continuation as a failed code ("We couldn't verify that code" forever — the operator's own account after enabling MFA, 2026-06-12). Fix: `verify_mfa` detects an accepted-but-unfinished payload (next-factor select/challenge, no messages) and returns the standard `mfa_required` hand-off with the SAME `login_id` (password kept as a factor with `method="password"`; webauthn-only → honest `mfa_unsupported`); `/auth/otp/verify` + `/auth/mfa/verify` pass the hand-off through (audited `mfa_pending`); all 3 surfaces re-enter the factor step from OTP verify, render a PASSWORD box (not a digits-only code box) for `method=password`, hide "Send a new code" there, and gate Verify on non-empty; chained continuations re-enter the menu. No-MFA accounts still complete in one step (success short-circuits before the continuation check) | ✅ offline `test_broker::test_otp_mfa_continuation` (fails pre-fix) + Playwright `reauth-dialog` (A29 test) + vitest `mfaCopy.test.ts` + live (operator's MFA account: code → password → enrolled) · 🔶 native mirrors via CI + AVD |
| A30 | **All displayed times are the viewer's LOCAL time + MFA option-button spacing** — the daily-sync schedule hour is STORED as ET (`sync_hour_et`; the cron gate runs in ET) but all 3 surfaces DISPLAY it converted to the device's local time (DST-safe, derived live from America/New_York: web `fmtEtHourLocal`, iOS Calendar-in-ET → local `DateFormatter`, Android `withZoneSameInstant`) with "(shown in your local time)" copy; the admin login-audit timestamps render via `fmtDateTime` (local) instead of the raw UTC ISO slice; the endpoint-health by-hour chips convert UTC buckets to local hours; the first-sync empty state computes the 7-ET default locally; the ReauthDialog body stacks prompts/fields/factor buttons with the login-form gap (`form-stack`) so MFA options never render flush (Android factor buttons: 6dp vertical padding) | ✅ vitest `dates.test.ts` (`etHourToday`/`fmtEtHourLocal` ET round-trip invariant — fails pre-fix: helpers absent) + Playwright `reauth-dialog` (existing, layout unchanged selectors) · 🔶 native schedule-label mirrors via CI + AVD spot-check |
| A21 | **Passwordless email-code login (OTP as the PRIMARY factor)** — `/auth/otp/start` answers `code_sent` ONLY after Okta accepted the email-factor select (probe-verified live: the real Okta offers `okta_email` on the primary menu, `tools/otp_probe.py` 2026-06-11); password-first account → honest 401 "use your password" + `otp_start_failed` audit (never a phantom "code sent" — the original bug); wrong code → friendly retry, pending survives; verify runs the same identity tail as MFA; the same account's password lane keeps working | ✅ e2e-mock `tests/e2e/test_otp_flows.py` (fails pre-fix: verify died on ImportError) |
| A31 | **WebView Church login — no password in our app** (2026-06-13, the user goal: "never enter the password in our app, only on the church webpage"). The native apps (iOS + Android) add **"Sign in on the Church website"** on the login screen AND **"Authorize on the Church website"** in the re-auth/enroll dialog: an in-app WKWebView/Android WebView loads the real `lcr.churchofjesuschrist.org/api/auth/login`, the leader authenticates ON churchofjesuschrist.org (password autofilled via Face-ID/fingerprint-unlocked keychain or typed there — never in our UI or on our server) with the **full Church MFA** (text/email/authenticator), and the app captures the resulting `*.churchofjesuschrist.org` cookies (Okta `sid` + LCR `appSession`, after ≥2 `appSession` cookies signal success) and posts them to the broker's existing `/auth/session`. The broker verifies the captured session and runs the same mint (login) / enroll (sync) completion as the password lane. The plain web-login adopts the view session directly (N2-guarded), NOT the post-login enroll offer (which re-auths with a password we never collect); sync setup is the explicit enroll button. Broker hardening: empty/non-Church cookie jar → honest `captured_no_cookies` 401 (never an empty-jar "stranger"); an LCR identity failure → `IdentityError` 503 (not a 401 reading like "bad sign-in"); per-IP rate limit; captures audited. **Web can't do this** (a browser can't read the Church's cross-origin cookies) → LoginPage + ReauthDialog show a note pointing to the mobile app. Install: `docs/IOS_SIDELOAD.md` (unsigned `.ipa` from CI → AltStore/Sideloadly on Windows, free Apple ID, 7-day refresh). True Face-ID/passkey AS AN MFA FACTOR can't run in an embedded WebView (Apple/Google block WebAuthn there) — keychain autofill + a code is the achievable, password-free end state | ✅ offline `test_broker::test_captured_session_verify` (empty/non-Church jar, Church-cookie filter, IdentityError passthrough, transient→503; fails pre-fix) · 🔶 native WebView capture (`ChurchWebAuthSheet.swift` / `ChurchWebAuth.kt`) via CI build + AVD/device pass (sign in on the Church page → app lands signed in; enroll stores the credential) |

| A32 | **The golden flow: one-MFA enroll → 45-day unattended sync → re-auth link** (2026-06-12, web). Minting the Member Tools sync token needs a Church session Okta will silently SSO from — which the broker's IDX login can't produce (Okta re-MFAs the Member Tools app). `web_session.py` drives the official-app path instead: `/api/v1/authn` (password→sessionToken, no MFA) → LCR `/authorize?sessionToken=` → Okta IDX widget (the ONE MFA code) → follow success → appSession, then `mint_from_okta_session` SSOs silently → the 45-day refresh token. So a single MFA yields the LCR session AND the sync token (proven live). Broker `/auth/web/{start,select,verify}` mirror the password/mfa endpoints; the minted token threads through `_login_eval` → `evaluate_and_maybe_store` → persisted (Supabase REST, encrypted) at enroll. Web + iOS + Android: the ReauthDialog/Sheet + the LoginPage post-login offer route to `/auth/web/*`. No stored password, no stored TOTP. `/reauth` deep-link route opens the dialog; the day-40 aging email links to it. | ✅ offline `tests/test_web_session.py` (one MFA → identity+cookies+token; wrong code friendly; no-MFA finishes) + live (one MFA → appSession + 45-day token; deployed `/auth/web/start` returns the factor menu) + Playwright `reauth-dialog` (password lane → `/auth/web/*`) + `login-enroll-offer` (offer → `/auth/web/start`) + `reauth-route` (`/reauth` opens the dialog) · 🔶 native enroll via CI build + device pass |

| A33 | **Church auth is username + password only — the passwordless OTP lane is removed** (2026-06-12, ADR-011; the "Ken Packer re-auth loop"). The emailed-code (OTP) Church lane signed a leader in but its Okta IDX session is **app-scoped**: it can't silently SSO to Member Tools, so it could never mint the 45-day sync token — it stored a token-less credential that failed every sync with `login_required`, and the app kept (uselessly) asking the OTP-enrolled leader to re-authorize. Removed across all surfaces: broker `/auth/otp/{start,verify}` + `okta_flow.otp_start/otp_verify` + `_OTP_*`; web `ReauthDialog` "Email code" mode + `broker.ts` `otpStart/otpVerify`; iOS `ReauthSheet`/`BrokerService`; Android `ReauthDialog`/`BrokerClient`; the `otpUsernameHint` copy on all 3 surfaces; the OTP unit + e2e tests. Re-auth dialogs are now password-only and **say so** ("Daily sync needs your username and password … an emailed code can sign you in to view, but can't authorize the background sync"). KEPT: passwordless **viewing** via the Supabase email relay (`/auth/email/*`, a different system) and the mobile **"Authorize on the Church website"** capture (also mints). | ✅ broker suite green after removal (238 checks; no `/auth/otp` route) + Playwright `reauth-dialog` is password-only (no "Email code" toggle; password lane → `/auth/web/*`) · 🔶 native re-auth password-only via CI build + device pass |

| A34 | **Friendly "password rejected" error + enroll-email idempotency** (2026-06-12, ADR-011 "Ken" follow-up). (a) Okta's `Password requirements were not met` (a rejected/expired password) fell through `friendly_auth_error` unclassified → the leader saw a raw, confusing dead-end and fell back to the OTP lane (which can't set up sync). Now classified `kind=password_rejected` with an actionable reset path ("…if it's correct, your account may need a password reset at churchofjesuschrist.org…"); raw kept as `root_cause`. (b) `_notify_enrolled` fired on EVERY enroll, so a leader who re-authorized repeatedly (an OTP enroll that kept failing to sync) got the "daily sync enabled" email each time (Ken Packer got 3 in 40 min; the secretary got his own). It now sends ONLY when the enroll establishes sync that wasn't there before (`active is None` — no usable/non-revoked credential prior); a redundant re-auth is silent (the in-app toast already confirms it). | ✅ offline `backend/test_broker.py::test_friendly_okta_errors` (password_rejected classified, names the reset path, not raw, root_cause kept) |

### B. Enrollment & credential lifecycle

| # | Scenario | Status |
|---|---|---|
| B1 | Consented enroll stores credential (eval → scrape → RPC) — **the 2026-06-10 NameError regression** | ✅ offline `test_broker::test_full_eval_enroll_reaches_scrape_and_stores` + e2e-mock |
| B2 | Re-enroll after REVOKE → row un-revoked, failure state cleared | ✅ e2e-mock (seeded revoked row) + offline RPC-semantics stub + **fullstack browser loop** (`apps/web/e2e/fullstack`). The 2026-06-12 enroll rewrite routed the post-login offer + re-auth through the credential-capture `/auth/web/*` lane (`web_session`: `/api/v1/authn` → `/authorize?sessionToken=` → appSession + silent Member Tools mint); the fullstack mock now implements that lane (`tests/mock_lcr` authn + sessionToken authorize + appSession cookie; `membertools.OKTA` derives from `IDENTITY_BASE` so the mint stays on the mock and cleanly no-ops). Pre-fix the browser loop stranded the leader on `/login` (the offer's `webStart` had no mock to talk to). |
| B3 | can_enroll offer for a credential-less stake (post-login offer, consent moved off login form) | ✅ offline + e2e-mock + Playwright `login-enroll-offer` |
| B4 | can_improve takeover offer (higher access replaces incomplete credential; lower never clobbers a higher one — even incomplete/failing, see B15) | ✅ offline (`onboarding.should_take_over`) · 🔶 e2e-mock persona pair |
| B5 | Consented enroll that stored NOTHING says so (dialog stays open with server error; no fake success toast) | ✅ Playwright `reauth-dialog` (web logic shipped eb7107f) |
| B6 | Slow access scrape → login answers within budget with enroll `{"deferred": true}`; the background eval still completes (audit lands) | ✅ `tests/e2e/test_deferred_report_offboard.py` (short-budget broker + `slow_access` scenario) |
| B7 | Stale credential (sync failed) → provider banner "Re-authorize"; non-provider "take it over" variant | ✅ Playwright `banners-empty-states` |
| B8 | Credential longevity: no refresh token ⇒ cookie-lifetime-bound. PRE-EMPTIVE aging nudge after a healthy sync (`CP_CREDENTIAL_AGE_ALERT_DAYS`, default 21, 0 disables; once per credential generation — re-enroll re-arms via `age_notified_at < updated_at`, migration 0047); stale alert fires once per failure streak | ✅ feature (`_maybe_age_alert`) + `tests/test_credential_and_email_gates.py` (B8 section) + F2 concurrency |
| B9 | Revoke: provider-only, idempotent ("already_revoked"), admin override path | ✅ live smoke + e2e-mock |
| B10 | Enrollment-status state machine none/active/stale/revoked incl. `is_provider`, no-role fallbacks | ✅ e2e-mock |
| B11 | Provider binding on enroll (0029 trigger): enroller immediately sees their whole stake | 🔶 matrix (seeded enroll → visibility) |
| B12 | Calling change: released leader loses access by next sync/login (cache heals via background refresh); newly-called heals the other way | ✅ offline (cache-refresh tests) · 🔶 matrix (provision_roles re-run flips visibility) |
| B13 | Passwordless OTP enroll (re-auth dialog "Email code" mode, all 3 surfaces): consented `otp/verify` stores the credential (same 0041 RPC semantics as B1) + audits `enrolled`; a plain OTP login never stores | ✅ e2e-mock `test_otp_flows.py` (full flow + no-store) · 🔶 native UI via CI builds + AVD spot-check |

| B16 | **OTP auto-submit on every form (2026-06-20; web + iOS + Android).** Every one-time code we accept is **6 digits** (Okta MFA — SMS/email/authenticator — AND the Supabase email sign-in code), so the field submits itself the instant the code is complete — whether the leader PASTES it (the value jumps to full length) or types the last digit — without also reaching for Verify. Web: shared `useOtpAutoSubmit(code, busy, submit)` hook (`OTP_LENGTH = 6`) on the LoginPage Church-MFA + email-code fields and the ReauthDialog code field; inputs digit-sanitize + cap at 6 so length 6 is an exact completeness test. Native: iOS `LoginView`/`ReauthSheet` `.onChange` and Android `AuthViewModel.onMfaCode`/`onEmailCode` + `ReauthDialog` `onValueChange` submit at 6 digits. Guards everywhere: never fires while a verify is in flight (`busy`/`isBusy`), never twice for the same code (the web `fired` ref + the natural single-fire of input events; iOS submits only on the already-normalized value so a >6-digit paste re-normalizes then fires once), and NEVER on a password second factor (no fixed length). | ✅ web vitest `otpAutoSubmit.test.ts` (paste→submit, typed→submit, partial→no-submit, busy→no-submit, no double-submit on re-render, re-arm after clear) + typecheck/test/build (167) · 🔶 iOS/Android `.onChange`/ViewModel auto-submit via CI + AVD |
| B15 | **STRICT most-elevated-wins + take-over UI — the Raleigh/Ricky incident (2026-06-18).** A rank-1000 complete credential (a Stake Assistant Clerk) had no refresh token, so its session aged out and a sync stamped `last_failed_at`; a rank-4 INCOMPLETE Stake High Councilor then enrolled and **overwrote** it, because the enroll RPC's `coverage incomplete` and `last_failed_at is not null` WHERE arms were rank-UNGATED (a lower leader could clobber a higher one). And the higher leader had **no UI** to reclaim: `SyncSettingsSheet` showed an active non-provider credential with no action. FIX: migration 0055 replaces only when **same leader refresh OR existing revoked OR incoming `access_rank` ≥ existing** (a strictly lower leader never wins, even vs an incomplete/failing holder; a revoked one is reclaimable by anyone so a stake is never permanently stuck). The broker re-reads post-RPC and returns `enroll_blocked:"downgrade"` (+`would_downgrade`/`existing_provider`) instead of a false success. `stake_credential_history` now persists every enroll/takeover/**refused-downgrade** (who provided, rank, whom they replaced). All three surfaces gained a "Use my account for sync" take-over action (`viewer_is_stake_leader` gate) for a non-provider stake leader on an active credential. | ✅ offline `scenario_enroll_most_elevated_wins` (4 flipped/added checks **fail pre-0055**) + supabase-stub `rpc_enroll_stake_credential` mirror + web vitest `sync_settings.test.tsx` (take-over button shown to a non-provider stake leader, hidden otherwise) + typecheck/test/build · 🔶 native take-over button via CI + AVD spot-check |

| B14 | **Refresh-patriarchal-blessing banner + signal + 2-week grace** (2026-06-13, items 1/2a). Patriarchal blessing is the ONE covenant-path field absent from the bulk `/api/v5/sync` (E11/E14) — it only fills from a per-member LCR `/mlt` profile read with a LIVE session (a re-auth). `admin.enrollment_status` exposes `patriarchal_pending` (count of REAL/baptized members still on the sentinel — investigators excluded so it can reach 0) + `credential.can_refresh_patriarchal` (calling can read profiles). Web `PatriarchalBanner` (`banner--info`) shows ONLY to the provider whose calling can read it, when pending>0, the credential is healthy, AND it hasn't been re-authed in 14 days (a one-time ordinance — fine to be 2 weeks stale; also fixes "I refreshed but the banner stayed"). Refresh → the existing ReauthDialog. KNOWN GAP (item 2a, deferred): the re-auth doesn't yet establish a live LCR session the `/mlt` fetch can use (`profile_ok=0`), so the refresh can't actually fill patriarchal until that's fixed. | ✅ offline `test_broker::test_enrollment_status_patriarchal_signal` (signal exposure, fails pre-fix) + web vitest `patriarchal_banner.test.tsx` (pluralized count + onRefresh) + typecheck/test/build · 🔶 native banner mirror via CI + AVD (deferred parity pass) |

### C. Sync settings, schedule, Drive

| # | Scenario | Status |
|---|---|---|
| C1 | Sheet actions follow credential state (revoked→Re-authorize only; stale→Re-authorize+Revoke; active provider→Sync now+Revoke; none→Set up) — **the "Revoked but offers Revoke" report** | ✅ web vitest `sync_settings.test.tsx` + Playwright `sync-settings` (parity: iOS/Android edits flagged for CI/AVD) |
| C2 | `/auth/sync-now` 409 on revoked credential (server gate behind the hidden button) | ✅ offline `test_broker` + e2e-mock |
| C3 | Sync-now: provider-only 403; partial-coverage warning surfaced | ✅ offline · ✅ e2e-mock |
| C4 | Schedule: ET-hour set / pause / resume; off-hour cron no-op; mid-day change takes effect same day | ✅ partial (broker get/set) · ⬜ TODO prepare-step gating unit test |
| C5 | Wipe data ≠ revoke (members deleted; credential + roles stay; repopulates) | ✅ offline `test_provider_wipe_data` |
| C6 | Drive connect/disconnect/needs-reconnect states; per-stake sheet | ✅ Playwright `sync-settings` (status rendering) · 🔶 fullstack OAuth loop |
| C7 | Sheet sharing = computed recipients EXACTLY (policy callings + missionaries; ward clerks/teachers never auto-shared; stake sees all wards; released leaders drop out); `reconcile_viewers` notifies ONLY newly-added viewers and never removes the owner/SA | ✅ `tests/test_sheet_sharing.py` (offline, mocked Drive) + read-only live audit `python tools/verify_sheet_sharing.py` (lists permissions only — zero shares/notifications) |
| C8 | The REVOKER sees the revoked state immediately (banner + settings refresh, no page reload) | ✅ fullstack lane (caught the missing `reloadEnrollStatus` after revoke, fixed 2026-06-11) |
| C9 | **Filters/sorts/view selections persist + a global "remember" switch** (item 9, web, local-only). Every table/view filter, sort, and view selection (Table sort + per-column value filters; Needs category/sort/org/ward; Golden Hour section/window/sort/org; KPIs period/unit/compare; Baptisms by-unit) is persisted in localStorage via `usePersistentState`, GATED by a global Settings switch "Remember my filters & view preferences" (default ON). The same switch governs the notes show/hide toggle (also in Settings, default SHOW). Turning the switch OFF clears all persisted prefs and stops persisting (per-session reset). Persistence is local only — no server | ✅ vitest `prefs.test.ts` (remember gating, off=clears+stops, view-pref round-trip, notes default/persist) + typecheck/build + Playwright `nav-and-gates` (tabs render with persisted state) · 🔶 native pref persistence mirrors via CI + AVD |

### D. Data visibility (RLS) & person views

| # | Scenario | Status |
|---|---|---|
| D1 | Stake leader sees all units; ward leader only theirs; no-role sees zero; anon sees zero | ✅ live smoke `test_rls` (extended 2026-06-25 to TWO stakes — whole-stake, ward, and EMAIL-keyed leaders each see ONLY their own stake, never the other; 8/8) + seeded matrix `tests/test_rls_matrix.py` (7/7 vs the test project, 2026-06-11) |
| D50 | **A cross-stake credential can never contaminate a stake** (2026-06-25; sync). The data-integrity guard refused only a credential whose registered unit was a CHILD ward of the token's Member-Tools root (Green Level Ward). A credential whose token resolves to an ENTIRELY DIFFERENT stake slipped through — an operator's own **Springville Utah** token enrolled as the **Raleigh** stake → the daily "Raleigh" job pulled Springville's members and stamped them with Raleigh's `stake_id`, so a Raleigh leader's table/baptisms/directory showed 34 Utah members under a "Raleigh" header (the reported leak). Generalized: the data's true stake (MT org root, else the resolved context) must equal the registered stake or the write is REFUSED; the caller auto-revokes the mis-scoped credential. Live cleanup removed the 34 contaminated rows + revoked the credential. | ✅ `pytest tests/test_sync_gating.py::test_cross_stake_credential_refuses_to_sync` (Springville-as-Raleigh refused before any write — **fails pre-fix**: the old guard only caught wards beneath the root) + full `test_sync_gating` (38) + live RLS isolation `test_rls` (D1) |
| D51 | **Table member name shortened to "First L"** (2026-06-25; web + iOS + Android). The table's Member column shows first name + last initial, dropping any middle name ("Kovaliov, Ilya James" → "Ilya K"); names are stored "Last, First [Middle…]". Sort + per-column filter still use the FULL name on every surface (the abbreviation is render-only). Web keeps its scroll-triggered shrink (new format); native applies it always (fixed-width column). | ✅ web `typecheck`/`build` green (`abbreviateName`) · 🔶 native iOS `tableShortName` / Android `shortMemberName` mirror via CI + AVD |
| D52 | **On-demand sync has a cooldown — no sync storms** (2026-06-25; broker + web). Diagnostics showed one stake (the operator's own) syncing 7-9×/day from repeated on-demand triggers (Sync-now / re-auth / re-enroll) while schedule-only stakes synced exactly once/day — the hourly cron + per-stake due-gate were NOT the cause (no config bug). `/auth/sync-now` now SKIPS dispatching when the stake synced within the last 5 minutes (returns `{status:"skipped"}`); the web shows "Already up to date" instead of starting a run. The daily schedule still guarantees one run/day. | ✅ broker ruff + web typecheck/build green; verified live that schedule-only stakes = 1 sync/day (St Petersburg, Springville) |
| D53 | **Owner-only maintenance mode** (2026-06-25; migration 0056 + web; native screen to follow). A single global switch (`app_settings`, single-row) the OWNER alone can flip (`set_maintenance_mode` RPC, owner-gated; `is_owner()` = the seeded owner email, NOT all admins). While ON, a RESTRICTIVE RLS policy on `members` + `manual_members` returns ZERO rows to everyone except the owner — containment that covers web AND native with no client code; OFF is a no-op. The web wraps the authed app in `MaintenanceGate`: non-owners get a lock screen, the owner gets a banner + an Admin toggle card with an optional message (read via the `maintenance_status` view, which never exposes the owner email). | ✅ live `test_rls` maintenance checks (owner keeps scope; non-owner + other-stake leader see NOTHING; OFF restores access — 12/12) + web typecheck/build green · 🔶 native maintenance screen mirrors via CI + AVD |
| D54 | **Localization (i18n) foundation — EN / ES / RU** (2026-06-25; web; native to follow). react-i18next wired in `apps/web/src/i18n` with English, Spanish, and Russian bundles; the active language persists (localStorage) and syncs onto `<html lang>` (a screen-reader/a11y win), with English fallback for any untranslated key so coverage grows incrementally + safely. A language picker lands in Settings → Appearance; the maintenance screen + owner banner/card are the first fully-localized slice. | ✅ web typecheck/build green; unit suite 168 pass (the 2 `kpis.sacramentWindow` reds are a pre-existing local-evening UTC/local date flake from commit 20de208 — green in CI/UTC, unrelated) · 🔶 full string coverage + native (Android strings.xml / iOS String Catalog) are the documented follow-up |
| D55 | **Cross-stake leak RECURRED — a delegated job wrote its members under the OPERATOR's stake (session contamination)** (2026-06-27; sync). The TRUE root cause (from the CI log of run 28300347398): the **Springville** job (`--stake 462373`) pulled Springville's 34 members from Springville's OWN Member-Tools token (correct), but its dead delegated LCR session was re-established as the **OPERATOR's Raleigh** session — so `sync_stake`'s `ctx = client.user_context()` resolved to **Raleigh (503991)** and the members were stamped with Raleigh's `stake_id` (units=8 Raleigh, roles=Raleigh's 130 ward positions). The trigger: the operator **disabled MFA**, so the previously-MFA-blocked headless `LCR_LOGIN` login now SUCCEEDS and contaminates a delegated job's session. The old code PREFERRED the live `user_context()` for the write identity; a foreign session therefore became the stake the members landed under. Fix (two layers): (a) **identity = the member DATA's own stake** — `sync_stake` picks the ctx whose `unit_number == expected_stake_unit` (the Member-Tools payload `ctx_override`), and a live session that resolved to a DIFFERENT stake is NEVER the write identity; (b) **LCR-only extras gated** — role provisioning / KPIs / the live missionary fallback run only when `live_session_matches` (the live session IS this stake's), so a foreign session can't clone the operator's callings/KPIs onto this stake. Plus the D55-original DATA-vs-identity guard (`_assert_members_belong_to_stake`, members' OWN units must belong to the write-target stake) as defense in depth. | ✅ `pytest tests/test_sync_gating.py` (43): `test_delegated_sync_writes_under_data_stake_not_foreign_session` (foreign Raleigh session + Springville data + expected=462373 → writes under **462373**, not 503991 — **fails pre-fix**: old identity wrote under the live session / would revoke the wrong credential) + `test_cross_stake_members_refused_when_unit_context_absent`/`_by_name` (member-data guard, fail pre-fix) + `_orphan_`/`_matching_` (no false refusal) + `tools/test_suite.py` (27) + F821 clean + live: 34 rows deleted, Raleigh re-synced to its real 106, Springville sync re-verified to write under 462373 |
| D56 | **A ⚠ "not available" table cell opens the fill-data side sheet** (2026-07-06; web — the client half of E22, native mirrored). Clicking any data-issue cell (or the patriarchal banner) opens `FillDataSheet` (Modal `side`): per-field missing counts from `GET /auth/profile-refresh/status`, the why (these fields need a live signed-in Church session — the daily sync can't reach them), and the fix — "Fill missing data now" → `POST /auth/profile-refresh`; a live stored session fills immediately (progress bar off the polled status; on the running→done edge the sheet reloads members ONCE so the ⚠s clear), a dead one answers `needs_reauth` → "Re-authorize & fill" opens the existing ReauthDialog (its enroll path runs the same fill; the sheet keeps polling underneath and picks the run up). Gates: non-stake-leaders get the "ask your stake leadership" explanation (no status call — the route would 403); an enrolled account without member-profile access gets the no-access explanation instead of the button. All sheet copy is i18n'd (EN/ES/RU `fillData.*`). | ✅ web vitest `fill_data_sheet.test.tsx` (non-leader no-call/no-button; leader sees per-field counts; needs_reauth → onReauth; fake-timer poll: progress → done fires onFilled exactly once; no-access hides the button — fails pre-fix: component absent) + typecheck/test (175)/build/e2e (48) · 🔶 native bottomsheet mirror via CI + AVD |
| D2 | Power user: exact scope clone (stake- and ward-level), recursive, escalation-safe, revoke removes | ✅ live smoke `test_power_users` + seeded matrix (clone scope) |
| D3 | Multi-stake email collision: same email roles in 2 stakes → union scope, no bleed | 🔶 matrix |
| D4 | Notes scoped by RLS; visible to same-scope leaders | ✅ Playwright `person-views` (render+save) · 🔶 matrix (cross-scope denial) |
| D16 | **Single editable note per member** (item 8, web + migration 0050). Dropped the thread/multi-comment model: ONE note per member in `member_notes` (unique on stake+member, RLS mirrored from members incl. the DBRLS-04 real-member bind). Edited INLINE — tap the note surface on the detail view (no separate "Edit" button) OR long-press a main-screen member row (`?editNote=1`). The full note shows on the row (multi-line, never truncated) with a global show/hide toggle (item 9). Legacy `member_comments` threads are KEPT and FOLDED into one text on read (author·date prefixes, oldest→newest) so nothing is lost; the first save writes the single field. `foldComments`/`buildNotesIndex` are pure | ✅ vitest `notes.test.ts` (fold order/fallbacks, single-note-wins, empty-clears, skips) + Playwright `person-views` (legacy thread folds in; edit→upsert to `member_notes`; long-press row → edit) · ⚠️ migration 0050 must be applied to prod · 🔶 native single-note editors mirror via CI + AVD |
| D17 | **Card polish** (item 10, web): all `.card`s share a subtle resting elevation (`--shadow-card`), rounded corners, and a gentle hover LIFT on tappable cards (`--shadow-card-hover` + 1px rise, pressed resets) via the shared tokens — so every SectionCard/KPI/stat/member-list card benefits at once. Light + dark elevation tokens added to `theme.css`; member rows get a small radius + smooth hover bg | ✅ typecheck/build + Playwright `person-views`/`sync-settings` (cards render, interactions unaffected) · 🔶 native card styling mirrors via CI + AVD |
| D18 | **Manual people being taught + merge** (item 11, web + migration 0051). A leader can ADD a friend/investigator to a unit (name + custom notes) before LCR has a record — stored in `manual_members` (RLS scoped like members; the daily sync never touches it). They show in the Being-Taught view under "Added by you". When the daily sync later brings a REAL record matching by NAME within the SAME UNIT, a **suggestion + Merge button** appears (never auto-merge). Merging is a FULL remote override EXCEPT the custom notes, which are PRESERVED onto the real member's single note (`mergedNote` appends under a divider, idempotent) and the manual row is soft-marked merged. `normalizeName`/`findMatch`/`mergeSuggestions`/`planMerge`/`mergedNote` are pure | ✅ vitest `manualMembers.test.ts` (name fold/order-insensitive, match-in-unit-only, unit_id precedence, merged-excluded, remote-overrides + notes-preserved + idempotent) + Playwright `person-views` (add→renders; matching record→Merge→PATCH merged + note upsert to the real uuid) · ⚠️ migration 0051 must be applied to prod · 🔶 native add/merge UI mirrors via CI + AVD |
| D5 | Member object variants render everywhere: full / all-null / empty-lists / investigator+goal-date / mid-milestones (no crashes, correct chips) | ✅ Playwright `person-views` fixtures |
| D6 | Admin-only tables (login_audit, access_audit) invisible to non-admins | ✅ live smoke `test_login_audit`/`test_admins` |
| D7 | **DBRLS-02 (audit 2026-06-11): provider-binding trigger hardened** — a `stake_credentials` write with no access posture (`access_rank`/`granting_role_ids` null) mints NO role; an access-bearing write binds stake-wide `stake_leader`; an uncorroborated bind (no `church_identities` row for email+unit) still binds but is flagged in `role_binding_audit` | ✅ live smoke `test_power_users` (DBRLS-02 checks, fail pre-0048) · 🔶 matrix |
| D8 | **DBRLS-03: revoke_power_user is inviter-or-admin only** — a co-leader who didn't issue an invite (and isn't an admin) can't revoke it; every revoke writes `power_user_audit` | ✅ live smoke `test_power_users` (DBRLS-03 check, fails pre-0048) |
| D9 | **DBRLS-04: member_comments INSERT bound to a real member** — a comment whose `member_person_uuid` isn't a member in the claimed (stake, unit) is rejected by the WITH CHECK | 🔶 matrix (`tests/test_rls_matrix.py`) |
| D7 | /report scoped to the caller's roles (stake-wide = all units; ward leader = their unit only, no leakage; no-role = empty "none" scope) | ✅ `tests/e2e/test_deferred_report_offboard.py` |
| D10 | **Baptism elapsed reads coarse** (web): the member row shows the baptism date + a relative duration with DAYS DROPPED once there's ≥1 month ("3 months", "1 month"), days only under a month ("5 days"). `baptismElapsed` keeps only the months segment of `monthsDaysAgo` (or the days segment under a month); future/unknown → empty | ✅ vitest `dates.test.ts` (`baptismElapsed` — fails pre-fix: helper absent) · 🔶 native MemberRow mirrors (`Elapsed.monthsDaysAgo` iOS / `DateParse.monthsDaysAgo` Android) via CI + AVD |
| D11 | **Sacrament attendance is the last-8-weeks window, not whole-history "missed"** (web): the detail view's Sacrament card shows "Attended X of Y over the last N weeks" (Y/N = the most recent up-to-8 weekly records; fewer when there's less data — a member who started 2 weeks ago reads "of 2"); the windowed dots render newest→ oldest L→R; the old "52 sacrament meetings missed" line (raw `sacramentMissed`/whole-history count) is GONE. `sacramentWindow` sorts newest-first by date (falls back to stored order when undated) and caps at 8 | ✅ vitest `kpis.test.ts` (`sacramentWindow`: 8-cap, fewer-weeks, date-sort, undated fallback — fails pre-fix: helper absent) + Playwright `person-views` ("Attended 2 of 3", title "Sacrament Attendance") · 🔶 native MemberRow/detail mirrors via CI + AVD |
| D12 | **N/A is excluded from missing/incomplete lists; patriarchal uses the temple-recommend age gate** (item 4, web + backend). (4a) Patriarchal blessing already gates on "turning ≥12 this calendar year" — the SAME `turns_at_least(12)` rule as temple recommend + the temple-name (family-name / first-temple-visit) milestones; pinned by a test asserting `patriarchalEligible === templeRecommendEligible`. (4b) A field whose value is **N/A** (priesthood for a woman, a patriarchal blessing for a young child) is EXCLUDED from every "needs / incomplete / completion-ring" surface — N/A ≠ not-done. New shared `isNA`/`expected`/`isMissing` (web `logic/milestones.ts`) + `_is_na`/field-aware `member_missing` (backend `milestones.py`); NeedsTab, GoldenHourTab, DrillSheet, `milestonesFor`/`completionOf`/`avgCompletion`, and the mailer's weekly-missing all route through them | ✅ web vitest `milestones.test.ts` (isNA, expected/isMissing exclude N/A, patriarchal===recommend gate, ring denominator drops N/A — fails pre-fix) + offline `tools/test_suite.py::test_milestone_na_exclusion` (woman's N/A priesthood + child's N/A steps never missing; real not-done still does — fails pre-fix) · 🔶 native Milestones ports mirror via CI + AVD |
| D13 | **Golden Hour chip tooltips** (item 5, web detail view): each labeled Golden Hour chip is a button that shows a tooltip on hover (CSS) AND click/tap (pinned, dismiss on outside-click/Escape) explaining what the step means + its status. The text is sourced from the SINGLE milestone definition's new `description` field (`logic/milestones.ts`), so all surfaces share one wording; circle chips' `title` is enriched with the same description | ✅ Playwright `person-views` (click a chip → its explanation tooltip visible) + vitest (descriptions present on every milestone via existing import) · 🔶 native chip tooltips/long-press mirror via CI + AVD |
| D14 | **Golden Hour completion is a full-width 2-column grid, no "…" truncation** (item 6, web): the "Golden Hour completion" card uses a CSS grid (`repeat(2, minmax(0,1fr))`, `grid-auto-rows: 1fr`) instead of fixed-124px flex tiles — it spans the full width (esp. on mobile), shows full milestone labels (wrap, never ellipsis), and equal-height rows keep the bars aligned when one label wraps to two lines | ✅ typecheck/build + Playwright `nav-and-gates` (Golden Hour tab renders) · 🔶 native completion grid mirrors via CI + AVD |
| D15 | **Principles Taught redesign** (item 7, web detail view): each lesson is its own block (label + "N/M taught", separated by a divider); each principle is a bordered circle — empty when not taught, FILLED when taught — and an interactive button that on hover/click/tap reveals the principle name + status (and a member-present note). A small legend (taught / not-yet / present) sits in the section header | ✅ Playwright `person-views` (lesson label + click a principle dot → its tooltip) · 🔶 native principles UI mirrors via CI + AVD |
| D8 | Ops console polish: section order (superseded by D19); Endpoint health card truncates to 10 rows + "View all" → paginated `/admin/endpoints`; login emails masked (first char + ≤4 `*`, domain visible) with a per-row eye toggle on both the console card and `/admin/logins` | ✅ web vitest `mask.test.ts` (mask rule) · typecheck/build/e2e · native = Admins reorder only, flagged for CI |
| D20 | **Natural LDS wording pass** (web — display strings only, no logic/behavior change). Machine-ish / awkward labels rewritten the way a bishop or stake president speaks: the Golden Hour milestone "Has ministers" → **"Ministers assigned"** (so the Needs view no longer reads "Needs Has ministers"); the Needs page header "Needs Action" → **"Action Needed"** with subtitle "Eligible members still working toward each step"; each Needs/category section + drill title "Needs {label}" / "Still need: {label}" → **"Still working toward: {label}"**; Baptisms tab "Prospective Baptisms" → **"Upcoming Baptisms"** ("No prospective baptisms…" → "No baptisms scheduled yet."); KPIs "Investigators at Sacrament" → **"Being Taught at Sacrament"**, "Lessons w/ member present" → "Lessons with a member present"; Table headers de-abbreviated ("Has min."→"Ministers assigned", "Gives min."→"Ministering others", "Melch."→"Melchizedek", "1st temple visit"→"First temple visit", "Recommend"→"Temple recommend", "Patriarchal"→"Patriarchal blessing"); milestone labels Title-cased ("Family Name Prepared", "First Temple Visit"); org bucket "Missionaries / WML" → "Missionaries & ward mission leader"; person-detail empty states "Not yet been given a calling." → "No calling yet." / "Not yet received a ministering assignment." → "No ministering assignment yet."; "investigator" dropped from leader-facing copy; "Baptised" → "Baptized" in Rules. Milestone meaning/abbr/eligibility UNCHANGED | ✅ web typecheck/test/build + Playwright `nav-and-gates` (Upcoming Baptisms, Action Needed — fails on the pre-rename copy) + full e2e suite green (Being Taught, milestone chips, person-views) · 🔶 native iOS/Android mirror the same strings via CI + AVD (parity list in the wording-pass report) |
| D21 | **Admin-only techy language for sync sentinels** (web; native flagged). The backend writes internal sentinels (`needs-profile-api`, `blocked: insufficient calling access`) into profile-gated columns when a field couldn't be fetched (so the merge preserves last-good). The Table tab leaked the RAW sentinel into every leader's cell, and the Person-detail collapsed it to "—" for EVERYONE (hiding it even from admins who need it to diagnose). New `isSentinel()` + `displayFieldValue(value, isAdmin, blank)` in `apps/web/src/lib/member.ts`: admins see the raw sentinel verbatim, regular leaders see friendly text ("Not available yet" on detail, "—" in the dense table); a real value (Yes/No/Active/date) passes through for both. `TableTab` routes the sentinel-able columns (baptism, priesthood, calling, both ministering, recommend, patriarchal) — and the per-column filter values, so filtering matches what's shown — through it via `useDashboard().isAdmin`; `PersonDetailPage`'s `disp(value, isAdmin)` + `StatusSections`/`RichBody` take `isAdmin`. (The `ListTextSection`/`NamesSection`/calling/ministering name sections were already safe — they render friendly empty text from `recordedYes` checks, never the raw sentinel.) | ✅ web vitest `member.test.ts` (`isSentinel`; `displayFieldValue` raw-to-admin / friendly-to-leader / real-value-passthrough — fails pre-fix: helper absent) + typecheck/test/build · 🔶 native iOS/Android mirror the helper + table/detail routing via CI + AVD (parity item C in `docs/NATIVE_PARITY_BACKLOG.md`) |
| D19 | **Ops console: token-expiry visibility + worklists + day-to-day reorder** (web only — the ops console has no native screen). (A1) the per-stake `self_renewing` chip read the OLD LCR `has_refresh_token` (~always false) → showed the OPPOSITE of truth; backend `_enrolled_stakes()` now derives `self_renewing = has_refresh_token OR membertools_refresh_enc` (the 45-day Member Tools token, migration 0049) and returns `membertools_minted_at` + a derived `token_expires_in_days` (45 − days since minted; null when no token, negative when expired). (B1) per-stake "45-day token: N days left" chip (green >10 / amber ≤10 / red expired-absent), list sorted soonest-to-expire first. (B2) "Needs re-authorization" worklist above Enrolled stakes, derived client-side from the same fetched array (stale/revoked/`needs_reauth`/token expired-or-absent) with provider email + a Send-re-auth-email button. (B3) `POST /admin/stakes/{id}/reauth-email` (require_admin) emails the credential provider the day-40 nudge + `/reauth` deep link (stake name HTML-escaped, 404 when no credential/provider email). (B4) "Signed in but sees nothing" worklist: `login_audit` rows `outcome=allowed`+`role_scope=none`, de-duped by email — the leaders to invite/bind. (B5) self-renewing rollup line (X self-renewing · Y manual · Z expiring within 7 days). (A2) enrolled-stakes action row wraps on a phone (name/ID on their own line, buttons in a `.wrap`). (A3) destructive Wipe/Remove separated + `btn--danger` (var(--error), dark-mode-safe). (A4) panel order System → Enrolled stakes → Recent logins → Admins → Diagnostics → Endpoint health → Actions → Calling overrides. (A5) Panel error hint is its own block element. (A6) FreshnessCard leads with the two timestamps emphasized, counts demoted under a "Counts" sublabel | ✅ offline `test_broker::test_enrolled_stakes_self_renewing_from_membertools` (self_renewing from the MT token, countdown +ve/null/-ve — fails pre-fix) + `::test_admin_reauth_email` (send + escape + 404 paths) + web typecheck/test/build · native N/A (no ops console screen) |

| D22 | **Baptisms card-per-person + Golden-Hour rows + ⚠ data-issue display** (2026-06-13, items 3/4/5; evolves D21). `logic/fieldDisplay.ts` is the one display contract used by table/cards/details: a real value shows; **"N/A" → quiet "not eligible"** (never a warning); the `needs-profile-api`/`blocked` sentinel OR null/empty → a **⚠ DATA ISSUE** (never the raw string; admins keep the sentinel text to diagnose) — pairs with the backend NULL-scrub (E15). Baptisms tab: a CARD PER PERSON (was one folded card) with full notes/comments, the person's Golden-Hour next steps, and their unit's missionaries; plus a second MISSIONARIES section per unit. Golden Hour: ONE ROW PER ITEM with ✓/○/⚠ icons, **N/A rows omitted** (`GoldenHourRows`/`goldenHourRows`). Friend detail: unit + unit missionaries + next steps. Missionary data is UNIT-level (`stakes.missionaries`); no per-investigator teaching link is synced (graceful empty state; backend follow-up). | ✅ web vitest `fieldDisplay.test.ts` (N/A vs sentinel/null vs real) + `goldenHourRows.test.tsx` (N/A omission + ⚠) + `milestones.test.ts` additions; web suite 137 green, typecheck/build · 🔶 native iOS/Android mirror `fieldDisplay` + the 3 views via CI + AVD (deferred parity pass) |
| D44 | **Each tab keeps its OWN scroll position** (2026-06-17, web; native flagged). The five+1 tab routes share the one `main` scroll container (it stays mounted across tab switches), so the scroll offset used to carry across tabs — scroll Golden Hour down, switch to Needs, and Needs opened mid-page. `DashboardShell` now records each tab's offset on scroll (keyed by pathname, in-memory) and restores it in a `useLayoutEffect` on tab entry (before paint, so no flash); a freshly-entered tab with no saved offset lands at the top. Overlay routes (settings/admin/person) aren't tabs, so they leave the backdrop's scroll untouched. | ✅ Playwright `nav-and-gates` ("each tab keeps its own scroll position": scroll Golden Hour → Needs opens at top (not the GH offset) → back to GH restores its own offset — **fails pre-fix**: the shared container carried the offset across tabs) + typecheck/test/build (157) · 🔶 native iOS/Android keep per-tab scroll natively (NavigationStack / saved LazyList state) — verify via CI + AVD |
| D45 | **Ops "Missing: [object Object]" fixed** (2026-06-18, web + iOS). The enrolled-stakes ops card rendered a credential's `coverage.missing` with `missing.join(', ')`, but each entry is a `{feature, granted_by, also_unnamed_roles}` OBJECT (`lcr_client/access.py:_clean_missing` → `onboarding.coverage_of` → `admin._enrolled_stakes`), so JS stringified each to the literal "[object Object]" (iOS `AdminView` stringified the dict to raw JSON). New `logic/coverage.ts:formatMissingFeatures` maps each entry to its feature label, tolerant of the legacy plain-string shape; `AdminPage` + iOS `AdminView` route through it. | ✅ web vitest `coverage.test.ts` (object→feature labels, never "[object Object]"; legacy strings passthrough; blanks/non-array) + typecheck/test/build (164) · 🔶 iOS AdminView mirror via CI |
| D46 | **Baptisms by-unit grouping no longer double-shows missionaries** (2026-06-18, web + iOS + Android). In the by-unit layout each unit SectionCard shows its missionaries once at the top (the unit-level strip), but every nested `BaptismPersonCard` ALSO rendered its own "Missionaries" section — so they appeared twice. The per-person card now takes a `nested` (web/iOS) / `embedded` (Android) flag and OMITS its Missionaries section when set; the by-date view (flag false) keeps the per-person strip. | ✅ web vitest `baptisms_nested.test.tsx` (per-card Missionaries present in by-date, absent in by-unit — fails pre-fix: the card always rendered it) + typecheck/test/build (164) · 🔶 native iOS (`nested`) + Android (`embedded`) gate the same block via CI + AVD |
| D47 | **Endpoint-health card stops crying "hot" on EXPECTED conditions** (2026-06-18 review; web + broker). The "Endpoint health (trend)" verdict was error-%-only, so dead/deprecated routes (member-list 404; the legacy one-work fan-out) and best-effort endpoints that 401 by design when the delegated LCR session lapses (user-context, dashboard/data — the 45-day Member Tools token still carries the core sync) all rendered as a scary "hot". `admin.endpoint_health` now CLASSIFIES each endpoint (`dead`/`deprecated`/`best_effort`/`load_bearing` via `_classify_endpoint`) and computes the verdict accordingly (`_endpoint_verdict`): dead/deprecated → `expected`, best-effort → `best_effort` (both muted in the UI), and only LOAD-BEARING endpoints go amber/red — on their SYNC error rate, splitting out the probe's deliberate stress-test of dead routes (the `kind` column was already selected but discarded). Web `AdminPage`/`AdminListPage` map the two new verdicts to a muted color, show the class on hover, and add a copy line explaining them. | ✅ `tests/test_endpoint_health.py` (classifier + verdict: dead@100%→expected, best_effort@26.5%→best_effort, load_bearing healthy/watch/hot — fails pre-fix: error-%-only returned "hot") + web typecheck/test/build (162) · native N/A (no ops console screen) |
| D49 | **Glimmer parity — content-shaped loading skeletons that seamlessly become the data** (2026-06-21, web; native to follow). Loading placeholders are being made to mirror each view's REAL layout so data swaps in with zero reflow (self-skeletonize: render the real shell with shimmer in the data slots). Batch 1 (the worst offenders): **Table tab** glimmer now renders the real `data-table` with the REAL column `<Header>`s (which need no data, so they anchor every column width) + shimmer cells shaped per column (name line / text value / status pill) — was `MemberListSkeleton` (avatar rows, wrong shape). **Person detail** gained a glimmer (was a bare "Loading…" line): the primary-container header card (avatar + name/meta lines), the Covenant Path section card (ring + labeled chips), and the two-column `detail-cols` of `SkeletonCard`s — all reuse the real `.card`/`.card__body`/`.section-card__head`/`.detail-cols` classes so margins + chrome are identical. ALL web views now glimmer over their real shells: **Needs** + **Golden Hour** (`MemberRowSkeleton` mirroring `.member-row` — avatar + name + meta + GH chips), **Leadership** (`Columns` of `SectionCardSkeleton` matching the unit-card grid; was the wrong avatar-list shape), **KPIs** (`KpisSkeleton` — the Overview 4-tile stat grid + 3 trend-chart cards), **Baptisms** (`Columns` of person-card `SectionCardSkeleton`s under the real `SectionTitle`). Two shared primitives added — `MemberRowSkeleton`, `SectionCardSkeleton`. Each loading branch gates on `d.loading && d.members.length === 0` so a populated re-render never flashes the glimmer. **Native:** iOS + Android already had matched `GoldenHourSkeleton` + `KpiSkeleton`; added `TableSkeleton` (frozen header + per-column cells at the real widths) and `BaptismsSkeleton` (section-title + per-person cards) and routed them per-tab in `DashboardView`/`DashboardScaffold` (Needs uses the member-row skeleton; Person detail receives the `Member` via navigation, so no loading glimmer). | ✅ web vitest `tableSkeleton.test.tsx` (loading renders `table.data-table` + real headers + `.skeleton`, NOT an avatar list — fails pre-fix) + typecheck/test/build (170 green) · 🔶 iOS/Android Table+Baptisms skeletons via CI + AVD |
| D48 | **Missionary avatars actually SHOW — re-attach from cache every sync + native photo rendering** (2026-06-20; backend + iOS + Android; finishes D43). D43's carry-forward + the bundle-pass `attach_missionary_photos` still left rosters with ZERO `photo_url` (live: 36 avatars cached in Storage `missionaries/<uuid>.jpg`, 0 attached) — the bundle attach only matches that run's bundle uuids, and a missionary photo cached on an earlier run never got re-applied. New `photos.attach_cached_missionary_photos(conn, stake_id)` reads `storage.objects` (one indexed query) for every roster missionary that has a cached avatar and writes a FRESH signed URL onto the roster — run on EVERY sync in `daily_sync.py` (not just the heavy `--photos` pass), so cached pictures always show and the 7-day signed URLs never silently expire (the whole point of caching to our own bucket vs. the bundle's short-lived Church CDN URLs). Ran once against prod → Raleigh 0→9 attached. Native parity: iOS `Missionary.photoURL` (CodingKeys `photo_url`) + Android `Missionary.photoUrl` (`@SerialName`) + the manual `missionariesByUnit` parse; both surfaces' missionary rows now render `PhotoAvatar`/`PhotoAvatar` (URL with initials fallback) instead of `InitialsAvatar`. Web already rendered `photo_url`. | ✅ backend `pytest tests/test_missionary_roster.py` (4) + photos import + ran live (Raleigh 9 attached) · 🔶 iOS/Android `Missionary.photoUrl` decode + `PhotoAvatar` render via CI + AVD |
| D43 | **Missionary avatars persist across syncs + senior couples hidden on the per-person Baptisms card** (2026-06-17; backend + web). (a) **Photos vanished.** Missionary avatars come from the periodic `--photos` bundle pass (`attach_missionary_photos`), but every plain daily sync rebuilds `stakes.missionaries` from the bulk companionships (which carry NO `photo_url`) via `update_stake_missionaries` — so each nightly run WIPED the avatars, while member photos survived (they live on the member row, not the roster JSON; `--photos` defaults to false on the schedule). Fix: `db.carry_forward_missionary_photos(prev, new)` re-applies the previous roster's `photo_url` by uuid before the roster is written (tolerant of the companionship + legacy flat shapes; a departed missionary's photo is dropped). **Plus** the `--photos` bundle pass — previously manual-dispatch only (`PHOTOS` defaulted false), so scheduled syncs never refreshed avatars and the 7-day signed URLs lapsed — now runs on the daily SCHEDULE by default (`PHOTOS: ${{ github.event.inputs.photos || 'true' }}`; manual dispatch still overrides). `daily_sync.py` wraps the whole photo pass in try/except so a 50MB-bundle hiccup can NEVER fail the (critical) data sync. Net: avatars pull on the next scheduled run and stay fresh. (b) **Senior missionaries on the Baptisms card.** On the per-person baptism card (the one with the baptismal date) `baptismCardMissionaries` drops SENIOR couples — a companionship whose missionaries share a surname (`isSeniorCouple`; names sync as "Surname, Given", with a "Given Surname" fallback) — and caps the card to ONE teaching companionship. Senior couples STILL appear in the "Missionaries by Unit" section below and the unit-level strips (only the person card filters). | ✅ backend `pytest tests/test_missionary_roster.py` (carry-forward by uuid; fresh value not overwritten; legacy/flat shape; departed-uuid dropped — **fails pre-fix**: helper absent) + ruff F821 · web vitest `missionaries.test.ts` (same-surname pair flagged senior, different-surname young kept, single not flagged, "Given Surname" order, senior dropped + capped-to-1, only-senior ⇒ empty) + typecheck/test/build (157) · 🔶 native iOS/Android mirror the senior filter on their baptism cards via CI + AVD (deferred parity pass) |
| D42 | **Side-sheets slide IN again + desktop card widths (Golden Hour 3/2/1 columns, Baptisms fit-content)** (2026-06-17, web desktop; native N/A). (a) The `side`/`sheet`/centered `Modal` entrance (`animation: side-in`/`sheet-up`/`dialog-in`) was killed early: `.dialog--entered { animation: none }` turns the entrance off once `entered` flips true, and `entered` was set by `onAnimationEnd` on the dialog `<div>` — but `animationend` **bubbles**, so the FIRST child animation to finish (or EVERY child under `prefers-reduced-motion`, where children complete in ~0.001ms) flipped `entered` mid-slide. The panel never finished sliding in (it appeared/snapped), while the slide-OUT on close still played because `.scrim.side.closing > .dialog--side` out-specifies `.dialog--entered` — the exact "don't slide in but slide out" report (Admin/Settings/person/report sheets). Fix: `onAnimationEnd` only flips `entered` for the dialog's OWN animation (`e.target === e.currentTarget`), ignoring bubbled child events. (b) Golden Hour unit cards (`UnitGrid` → `Columns`) showed 3 too-narrow columns inside the 960px-capped page (the metadata line wrapped). `Columns` keeps its **balanced round-robin** stacks (no dead space under a shorter column) but, when given `minColWidth` (UnitGrid passes 350), derives the column COUNT from the **measured** container width via `ResizeObserver` — as many ≥350px columns as fit, capped at `colsFor(tier)`: a wide desktop shows 3, stepping to 2 then 1 as the window narrows. The Golden Hour pages widen (960 → 1140) so 3 columns of ≥350px fit. (c) Baptisms (by-date) cards stretched edge-to-edge (`flex:1` in a full-width column). The list (`.baptism-card-list`) is now a single `grid-template-columns: fit-content(600px)` track with `justify-content: start` on tablet/desktop (`min-width:600px`): the column shrinks to the widest card's content (capped 600px) and every row matches it — content-sized, not full-width; phones keep the full-width flex column. | ✅ web vitest `modal.test.tsx` (a child `animationend` must NOT mark the dialog entered; only its own does — **fails pre-fix**: bubbled child event flipped `entered`) + typecheck/test/build (150) + Playwright `nav-and-gates` (Golden Hour "Recently Baptized" renders) · native N/A (web Modal animation + desktop multi-column/desktop-width layout; native uses its own sheet presentation + single-column phone layout) |
| D41 | **Ops dash: per-day "Field gaps" aggregation + honest "recorded yes" re-auth copy** (track-missing-fields, web + telemetry already in backend). Answers "why is Dewey missing ministering assignment?" — he ISN'T (his flat `ministering_assignment="Yes"`); only the assignment's *detail names* (the per-member `/mlt` profile) are unavailable in steady state, so the person-detail note now reads **"Recorded as yes — the names come from LCR's per-member detail, which needs a live Church session. Re-authorize to refresh them."** (was a misleading "will appear on a future sync" — a plain daily sync can't refresh a re-auth-only field; matches the "missing member data" banner). **Logging:** every membertools sync already emits ONE PII-safe Axiom `sync.field_gaps` event + stamps `run_stats.field_gaps` (per-field members-missing + WHY) into the diagnostics row (`report._emit_field_gap_report`, wired in `build_membertools_report`). **New ops view:** an admin "Field gaps (per day)" panel (middle of the console) reads the recent diagnostics rows and rolls them up **per calendar day across stakes** (`logic/fieldGaps.ts:fieldGapSeries` — latest run per stake per day, summed members-missing per field), rendering each field as a sparkline + current count + ▲/▼ trend + the reason, so a leader's blank field is a number you watch fall. | ✅ web vitest `fieldGaps.test.ts` (per-day grouping/sum across stakes, latest-run-per-stake-per-day de-dup, field_coverage fallback, worst-first sort, empty — **fails pre-fix**: helper absent) + typecheck/test/build (149) · backend telemetry already shipped (`report._emit_field_gap_report` → Axiom `sync.field_gaps`) · native N/A (ops console + re-auth note are web; native mirrors the note copy via CI) |
| D57 | **Ops "Field gaps" scoped by stake** (2026-07-06; reported "field gaps in ops — allow visibility by stake"). The per-day field-gap rollup (D41) was all-stakes-only; the diagnostics rows already carry `stake_id`, so `fieldGapSeries` gains an optional `stakeId` filter (pure client-side, same fetch) and `FieldGapsCard` a stake `<select>` (All stakes + each ENROLLED stake that actually has sync telemetry — an option that renders an empty card reads like a bug). The panel now loads diagnostics + enrolled-stakes together (names for the selector); the trailing scope label and the card copy follow the selection. | ✅ web vitest `fieldGaps.test.ts::scopes to ONE stake` (per-stake totals, other-stake fields excluded, day list shrinks to the stake's runs, ''/null = unscoped — fails pre-fix: param absent) + typecheck/test/build |
| D40 | **Table: every ENUMERABLE tracked field is filterable — sex filter added (web parity with native).** The Table tab's `FILTERABLE` allowlist (web `TableTab.tsx`) lumped **sex** in with the continuous columns, so a leader could sort by sex but not FILTER ("show only sisters/brothers"). Sex is a tiny M/F enum like the yes/no/recommend status columns, so it now gets a per-value filter; the genuinely continuous columns (name, age, baptism date, "Member for", friends-count) stay sort-only by design (a per-value checkbox there is just noise). The native iOS (`TableView.swift`) + Android (`TableScreen.kt`) tables already render a filter button on **every** column, so this brings WEB into parity — no native change needed. `COLS`/`FILTERABLE` are exported so a unit test pins the contract: every enumerable column ⇒ filterable, every continuous column ⇒ sort-only, every FILTERABLE key is a real column. **Export already complete:** the Google-Sheet mirror (`sheets_sync/row_mapper.py`) emits all 17 covenant-path columns (the 13 fields + member-for/age/first-temple-visit/family-name), so no export gap to fill. | ✅ web vitest `tableFilters.test.ts` (sex filterable; enumerable⇒filter / continuous⇒sort-only contract; FILTERABLE⊆COLS — **fails pre-fix**: sex absent from FILTERABLE) + typecheck/test/build (144) · native already filters all columns (no change) |
| D39 | **Section-completeness borders (#10c) + a live "Refined" theme toggle (#6a commit-2)** (web; native flagged). `SectionCard` gains a `tone` (`good`/`warn`/`bad`) → a green/amber/red **left-edge border**; the person-detail Covenant-Path card uses it (green at 100% complete, amber in progress, red at none) alongside its completion ring (not color-only). The theming **commit-2 showcase** ships as a live, reversible toggle: `useTheme` adds a `look` (`classic`/`refined`) persisted as `data-look` on `<html>`; `theme.css` `[data-look='refined']` retunes the app CHROME (a richer primary, cleaner surfaces, softer radii) — **keeping the load-bearing milestone/unit/org identity colors** — so you flip it in **Settings → Appearance → Style** to compare and keep whichever you prefer. | ✅ web typecheck/test/build (148) · 🔶 native mirror the section tone; the Refined palette is web-only until chosen |
| D38 | **Notes are a TIMESTAMPED THREAD again — add / edit / delete by unit leaders (web + migration 0054; native flagged).** Reverses the single-note model (D16) per user feedback. `member_comments` (the 0017 thread) is revived as the source of truth: migration `0054` adds `updated_at`/`updated_by` + widens the RLS so ANY in-scope leader (the member's stake/unit) can EDIT and DELETE every entry (not just the author). `NotesSection` is a full thread: an always-available add field, entries newest-first with **author · timestamp** (+ "(edited)"), per-entry **Edit/Delete**, and **Clear all**. On summary cards `NoteLine` shows the latest entries with a relative timestamp + "+N more" (tap the row → the detail thread; long-press → focused on the add field). `useDashboard` now exposes `threads` (member_comments, newest-first); the single-note `member_notes` is kept for the manual-merge preserved note. | ✅ web typecheck/test/build (148) + Playwright `person-views` (existing entry shows; add → POST member_comments + read-after-write; delete → DELETE; long-press → add field) — 10/10 + e2e mocks extended (member_comments PATCH/DELETE, `id`) + migration `0054` applied to prod · 🔶 native note editors mirror the thread via CI + AVD (deferred) |
| D37 | **Missionaries by COMPANIONSHIP, with photos (#3a, web + backend; native flagged).** The bulk `missionariesAssigned` is an array of companionships — `membertools_adapter.missionaries_by_unit()` extracts each as `{phone, email, mission_name, missionaries:[{uuid,name,email,type}]}` expanded across the units it serves (session-independent; threaded through `build_membertools_report` stats → `sync_stake` maps unit-number→name → `stakes.missionaries`, preferring this over the old `/mlt` fetch). `db.attach_missionary_photos()` sets `photo_url` on each missionary from the `/api/v5/sync/files` bundle map (E17). `components/Missionaries.tsx` rebuilt: `MissionaryStrip`/`MissionariesSection` group by **companionship** with avatar **photos** + name/type and the **shared phone/email** (tel:/mailto:), tolerant of the old flat shape. Used on Baptisms cards, the Leadership tab, and person detail. | ✅ `tests/test_membertools_adapter.py::test_missionaries_by_unit_companionships` (companionship grouping, shared contact, multi-unit expand, mission-wide skip — fails pre-fix) + web typecheck/build + validated LIVE on 503991 (6 units with companionships stored; 10/11 missionary photos attached) · 🔶 native mirrors via CI + AVD (deferred) |
| D35 | **Needs polish: custom icon/color dropdowns, name/age on separate lines, gender, elapsed baptism, switch toggle** (web; native flagged). The Category + Units filters are now a custom animated `Dropdown` (`components/Dropdown.tsx`) instead of plain native `<select>`s — each option shows the milestone icon+color (with its outstanding count) / the unit pill color; accessible listbox (arrow keys, Enter/Space, Escape, outside-click, typeahead) with an eased open animation. On member cards: the **name sits on its own line** with **gender (M/F chip) + age on the line below** (the `.member-row__main` is now a column — they used to share a line); the **baptism reads as elapsed time** ("3 months ago", no literal date — `MemberRow` `elapsedBaptism` now defaults true). The "Unit snapshot" control is now a proper `.switch` toggle, not a bare checkbox. | ✅ web typecheck/test/build · 🔶 native iOS/Android mirror the dropdown/card layout via CI + AVD (deferred parity pass) |
| D36 | **Side-sheets polish: eased open/close, in-sheet loading, URL-landable Settings/Admin sheets, account rights** (#6, web; native flagged). The shared `Modal` now animates IN and OUT with an iOS-style cubic-bezier ease — side sheets slide from the right (+fade), bottom sheets slide up, and the scrim fades; on close it keeps the dialog mounted, plays the matching `*-out` keyframe (~220–260ms), then unmounts (a `closing`/`render` state + a fallback timeout). Focus-trap / Escape / scrim-dismiss / focus-restore are unchanged, and `prefers-reduced-motion` users get an instant show/hide (theme.css zeroes the durations; the unmount-timeout still fires) with no stuck state. Loading moved INSIDE sheets: a new `loading` Modal prop renders a centered spinner over the body — the **report flow** opens its side sheet immediately and shows the spinner in it (the old full-screen `scrim` block is removed), and the Admin console lazy-chunk shows an in-sheet spinner. **Settings + Admin are now URL-landable side sheets OVER the dashboard** (not standalone pages): `/settings`, `/admin`, `/admin/:section` are children of `DashboardShell` that render the default tab underneath while the shell overlays the matching sheet (keyed off the pathname), so a deep link / refresh on `/settings` or `/admin` lands on the dashboard with the sheet open; the menu "Settings"/"Admin" navigate to those routes (URL updates; Back/forward + refresh work); Esc/scrim/× closes the sheet and navigates back to the underlying tab. Admin opens as a **WIDER** side sheet (`wide` → `min(880px,96vw)`) for its dense tables — cards untouched; `AdminPage` renders `embedded` (drops its own app-shell chrome) and `AdminListPage` renders in a `bare` sheet so its own app-bar is the header. Settings' Account section now shows the signed-in user's **stake / unit(s) / calling(s) + the rights each grants** ("Stake President — Sees all units in {Stake}" vs "Bishop — Sees {Unit} only"), queried from the RLS-scoped `user_roles` (+ `units`/`stakes` joins), grouped by stake, with an honest power-user note when there's no role and an "App admin" badge when `isAdmin`. | ✅ web typecheck/test/build (148) + Playwright `side-sheets` (menu opens Settings sheet + URL=/settings + nav underneath; Esc/× returns to /baptisms; deep-link refresh lands dashboard+sheet; stake-leader "Sees all units" vs ward-leader "Sees {Unit} only" vs power-user note; Admin wide sheet asserts `dialog--wide` + landable) + `nav-and-gates`/`sync-settings`/`person-views` green (52 e2e) · 🔶 native iOS/Android: settings/admin presentation + account-rights are a deferred parity pass (CI + AVD) |
| D34 | **Theming consolidation (commit 1, keep identity)** (#6a, web). Centralized the scattered color literals into the one theme source (`theme/tokens.ts`): the data-table cell fills (`cell.yes/no/na/amber`, was re-declared in TableTab) and the sacrament-attendance level→hue map (`attendanceColor(level)`, was duplicated in `Badges.tsx` + `PersonDetailPage.tsx`) now live in tokens with documented contrast roles. **No color VALUES changed** — pure de-duplication so there's one place to tune (the identity-preserving "commit 1"; the full-redesign "commit 2" showcase is a follow-up that needs the user's visual choice). | ✅ web typecheck/test/build (148) — no behavior/visual change · 🔶 native palettes already single-sourced (`Color.kt`/`Theme.swift`); a contrast pass + the redesign showcase are the deferred commit-2 work |
| D33 | **New Leadership tab — per-unit staffing gaps + missionaries** (#12 + #3a, web; backend + native flagged). A 6th top-level tab ("Leaders") shows, per unit: a **required-role checklist** (ward mission leader, ≥2 ward missionaries, EQ president, RS president) with ✓ filled + holder names or a ⚠ gap, a **"Currently serving"** list of the unit's tracked leadership callings, and the **assigned missionaries**. Backend: `membertools_adapter.staffing_by_unit(payload)` extracts the leadership-relevant callings from the bulk household directory (`households[].members[].positions`, grouped by the POSITION's `unitNumber`), threaded through `build_membertools_report` stats and stored on **`units.staffing`** (migration `0053`) — chosen because the existing `units_select` RLS already scopes it **per unit** (stake leaders see all units, a ward leader sees only theirs; no new policy, no app-side filtering). Shared `logic/staffing.ts` (`staffingGaps`/`gapCount` + the `REQUIRED_ROLES` rule) is pure → mirrored. Session-independent (survives a dead LCR session). Companionship/photos remain unavailable (#3d, documented). | ✅ backend `tests/test_membertools_adapter.py::test_staffing_by_unit_groups_tracked_callings_by_position_unit` (tracked-only, grouped by position unit — fails pre-fix: function absent) + validated on the live 503991 payload (13 units, real callings) + migration `0053` applied to prod · web vitest `staffing.test.ts` (gap rule: filled/gap, assistant≠WML, 1-of-2 missionaries, fully-staffed — fails pre-fix) + typecheck/test/build (148) + Playwright `nav-and-gates` (6th tab navigates) · 🔶 native iOS/Android mirror `staffingGaps` + the Leadership screen (6th-tab nav per platform) via CI + AVD (deferred parity pass) |
| D32 | **Ops console: System health + Tools merged into one deeplink-per-system panel** (#7, web only — no native ops screen). The separate "System health" (status pills) and "Tools & dashboards" (link chips) cards are merged into one **"System health & tools"** panel: each system is its own row with a status dot (Broker/Render, Supabase, GitHub Actions — where we know it), a useful live stat where available (Supabase member count, GitHub linked/not), and an **"Open" deeplink** (Render, Supabase, Axiom/logs, Sentry/errors, GitHub Actions, plus any other configured links) — so an admin can jump straight into each system. Maintenance actions (#7b) + the denser Diagnostics (#7c) were already reworked in D19 and remain valid. | ✅ web typecheck/test/build (145) · native N/A |
| D31 | **Sheets slide in from the RIGHT on desktop, bottom on mobile** (#6b, web; native flagged). The shared `Modal` gains a `side` variant: a full-height panel anchored to the **right** on tablet/desktop (slides in via `side-in`; the previous screen stays visible-but-dimmed through the scrim), falling back to a **bottom sheet** on phones (`max-width:600px`) where a right side-sheet is the wrong Material/HIG pattern. Sync settings, Report, and Power-user now use `side`. Focus-trap / Escape / scrim-dismiss / focus-restore are unchanged; the slide animation is disabled under `prefers-reduced-motion` (global rule). | ✅ web typecheck/test/build (145) + Playwright `sync-settings` (6/6 — sheet opens/acts/closes) · 🔶 native iOS = `.sheet` detents / trailing column, Android = side sheet (expanded) / `ModalBottomSheet` (compact) via CI + AVD (deferred parity pass) |
| D30 | **Invite a power user is now a compact SHEET, not a full page** (#5, web; native flagged). "Invite a power user" in the dashboard menu now opens a `PowerUserSheet` (the same Modal sheet chrome as Sync settings) instead of navigating to the `/invite` page — the description, unit-scope select, email+Invite row, status message, and the invited-users list (with Revoke) all render compactly in the sheet. The shared `PowerUserBody` is reused by both the sheet and the still-present `/invite` route (deep links). RPCs (`invite_power_user`/`revoke_power_user`) unchanged. | ✅ web typecheck/test/build (145) · 🔶 native iOS/Android mirror the power-user sheet via CI + AVD (deferred parity pass) |
| D29 | **Baptisms: single column, date pill outside the card on the left, notes/path prioritized** (#3, web; native flagged). The upcoming-baptisms list is now a **single column** of `[date pill | person card]` rows (#3c): a narrow date pill (month/day/weekday + countdown) sits **outside the card on the left** (#3e), the card to its right with a colored left accent. The card **leads with the leader's notes/concerns** (empty-state prompts to add one), then the **"Path to baptism"** steps; the covenant-path Golden-Hour milestone pills are **removed** here (#3b — investigators are being taught, not yet on the covenant path). The per-unit missionaries strip + the Missionaries-by-Unit section are unchanged. (#3d group-by-companionship is NOT possible — companionship isn't in any data source; documented in `docs/BULK_PAYLOAD_FEATURES.md`.) | ✅ web typecheck/test/build (145) + Playwright `nav-and-gates` (Upcoming Baptisms renders) · 🔶 native iOS/Android mirror the single-column date-pill layout via CI + AVD (deferred parity pass) |
| D28 | **Report sheet: narrow by unit + category before reading** (#4, web; native flagged). The convert-integration report (broker-built) now has two **never-empty multi-select chip rows** — Units and Categories/steps — that filter the on-screen report client-side: the "members need attention" count, the most-needed-steps list, and the per-member outstanding list all recompute to the selection (a member's missing steps are narrowed to the chosen categories), with a "(filtered)" hint; deselecting the last chip re-selects all. Cleaner layout; "Email full report to me" still sends the stake-wide report via the broker. | ✅ web typecheck/test/build (145) · 🔶 native iOS/Android mirror the report filters via CI + AVD (deferred parity pass) |
| D27 | **Person detail: de-duplicated covenant-path chips (⚠ folded in), connected sacrament timeline, proper notes font** (#10, web; native flagged). The Covenant Path section now shows ONE representation — the labeled chips — each carrying its `goldenHourRows` 3-way status (✓ done / ○ not yet / **⚠ data issue**) plus a tap tooltip; the duplicate "done-text" `GoldenHourRows` block is removed from the detail view (#10a — the component stays for Baptisms + native; the ⚠ signal moved onto the chip so nothing regresses, cf. D22). Sacrament attendance is now a **connected timeline** (a line threads the weekly markers; **present = filled ✓, absent = hollow ✕** — shape, not color alone, WCAG 1.4.1) that scrolls horizontally on a phone, with a health-colored "attended N of M" summary + an attendance pill in the header (#10b). The Notes editor (and all inputs/selects) now `font: inherit` instead of the browser's monospace UA font (#10e). N/A sections/steps remain omitted (eligibility-gated, D12); principle dots keep their bordered radio look + tap tooltips (D15). Photos remain re-auth-only (documented gap, #10f/#11). **Person detail now opens as a URL-landable side/bottom sheet OVER the current tab** (like Settings/Admin) instead of a standalone full page: `/person/:id` is a `DashboardShell` child whose element is `BackgroundTab` (the last tab as backdrop), and the shell overlays a `Modal side wide` hosting `PersonDetailBody` — title = the member's name, the "open in LCR" link moved to the Modal `headerExtra`, close ⇒ back to the underlying tab. Table/list row → person opens the same sheet (the `navigate('/person/:id')` targets are unchanged). | ✅ web typecheck/test/build (140) + Playwright `person-views` (chip tooltip, principle dots, "Attended 2 of 3", completion ring, notes edit, long-press editNote — 10/10 against the sheet) · 🔶 native iOS/Android mirror the chip ⚠ fold + timeline + input font via CI + AVD (deferred parity pass) |
| D26 | **Golden Hour: per-unit completion rings + category icons + WML/RS/EQ abbreviations + elapsed baptism + age-below-name** (#8/#8.1, web; native flagged). New **per-unit indicators** card (`unitGoldenHour`): for each unit a **SECTIONED ring** (one arc per person, filled when they're fully complete = % of people done) and a **FILLED ring** (% of eligible items complete overall), each with a numeric centre label + a one-line legend (`components/Rings.tsx`, accessible `role=img`/aria-label, distinct treatments). The completion card shows each milestone's **icon + color** next to its rate (#8a). The shared `OrgFilterBar` now shows **WML/RS/EQ shorthand** (full name in the tooltip) instead of long labels (#8c/#8.1c). On Golden Hour person cards the baptism reads as **elapsed time** ("3 months ago"), not the literal date (#8.1a), and **age sits below the name** everywhere (#8.1b, MemberRow). | ✅ web vitest `milestones.test.ts` (`unitGoldenHour`: group-by-unit people-desc, fully-complete count, items done/total — fails pre-fix: helper absent) + typecheck/test/build (145) · 🔶 native iOS/Android mirror `unitGoldenHour` + the rings/abbr/elapsed/age via CI + AVD (deferred parity pass) |
| D25 | **Navbar Refresh button: real tooltip + visible pending state** (#2, web; native flagged). The Refresh button gave no feedback (felt like it "did nothing"). It now carries a clearer tooltip ("Refresh data") and, while reloading, swaps to a `Spinner` with `aria-busy` so the action is visible; `useDashboard.refresh()` sets a new `refreshing` flag (separate from the full-page `loading`) and ALSO re-pulls the stake freshness (`reloadStakes`) so "Updated X ago" reflects the latest run. | ✅ web typecheck/test/build (144) · 🔶 native iOS/Android mirror the refresh affordance via CI + AVD |
| D24 | **Table: sort every column + click-the-title to sort, filters only where enumerable, instant-apply, "Member for" formatting, scroll-pin border** (#9, web; native flagged). Every column now SORTS (age & friends numerically, baptism & "Member for" by the baptism instant, the rest by text) and the column **TITLE itself is a sort button** (`.th-label`), alongside the arrow. The per-value **filter** button shows only on **enumerable** columns (unit, sex, + the yes/no/recommend status fields; sex added in D40); age/baptism/member-for/friends/name sort-only. The filter popover **applies immediately** (no Apply/Cancel — toggling a value updates the table live, with an `aria-live` "N values hidden") and a single **Done** closes it. **"Member for"** is recomputed from the baptism date via `tenure()` — "2 years 3 months" with any zero part dropped (never "0 years"/"0 months"), days only under 2 months. Sticky-column freeze reworked from `nth-child` to **classes** (`.col-num`/`.col-member`) so hiding the **# column on phones** (`max-width:600px`) no longer shifts the Member offset; horizontal scroll adds a **semibold right border** on the pinned Member column (`.table-wrap.is-scrolled`). Header buttons get a ≥40px touch target on phones (#9h). | ✅ web vitest `dates.test.ts` (`tenure`: years+months zero-drop, sub-2-month days, null/future — fails pre-fix: helper absent) + typecheck/test/build (144 green) · 🔶 native iOS/Android mirror `tenure` + the table sort/filter/freeze via CI + AVD (deferred parity pass) |
| D23 | **Needs view: single filter row + shorthand org badges + sacrament-attendance category** (#1, web; native flagged). The Needs filters collapse into ONE row — a **Category dropdown** (every milestone + the new **Sacrament attendance** category, each with its outstanding count), a **Units dropdown** ("Stake — all units" default), and the WML/EQ/RS **org toggle chips** (never-empty: deselecting the last re-selects all). Added a **group-by-unit** segmented toggle + asc/desc, and a **Unit snapshot** checkbox that hides the per-unit count pills. Person cards now show the org as a **WML/RS/EQ shorthand badge** (`OrgBadge`, color + "SHORT — Long" tooltip) instead of the long label, and (when relevant) a **sacrament-attendance row above it** (`AttendancePill`: "Sacrament 7/8" colored by health — 8/7 green, 4–6 orange, 1–3 red, **0 bold + red ⓘ**, no-data muted). New shared `attendanceBucket`/`memberAttendance` in `logic/kpis.ts` (semantic level only; the UI maps level→color) + `components/Badges.tsx`. | ✅ web vitest `kpis.test.ts` (`attendanceBucket`: 8/7→great, 4–6→fair, 1–3→poor, 0→none+bold, no-data→unknown — fails pre-fix: helper absent) + typecheck/test/build (141 green) · 🔶 native iOS/Android mirror `attendanceBucket` + the Needs filter row/badges via CI + AVD (deferred parity pass) |

### E. Sync & data integrity (backend jobs)

| # | Scenario | Status |
|---|---|---|
| E1 | Reconcile (hard-delete departed) defers during degraded runs (>3 candidates + failed unit) | ✅ live smoke `test_reconcile` |
| E2 | Field-meta staleness: profile-denied runs preserve good data, mark stale | ✅ `test_field_meta` |
| E3 | Per-stake scoped dispatch (enroll kickoff + sync-now never fan out) | ✅ offline (dispatch allowlist) + code-asserted inputs |
| E4 | Daily-sync delay-robust gating (due-by-hour, late-cron fires, paused, failed-run-stays-due) | ✅ `tests/test_sync_gating.py` (offline) |
| E5 | `_revoke_if_ineligible` re-verifies callings each sync (revokes ONLY on a positive no-access read; inconclusive/error never revokes; stewardship safety net) | ✅ `tests/test_sync_gating.py` (offline) |
| E6 | **Member Tools token persistence — the 2026-06-12 "re-authorize loop" fix.** The covenant-path bulk sync (`/api/v5/sync`) bearer is minted by a SILENT prompt=none authorize that needs a LIVE Okta WEB session — which a delegated stake's stored credential loses within hours/days (long-lived LCR appSession survives, but the Okta `sid` dies), so `mint_from_okta_session` returns `login_required` and the stake stops syncing ("as soon as I enroll it asks me to re-authorize", stake 503991). Member Tools issues a **45-day refresh token** that renews the bearer with NO Okta session, but it was only cached in the local file store (`delegated_grants.enc`), which does NOT survive the ephemeral CI runners → never accumulated. Fix: persist it **Fernet-encrypted in Supabase** (`stake_credentials.membertools_refresh_enc`, migration 0049), **bootstrapped at ENROLL** while the session is fresh+mintable; `_membertools_access_token` prefers the stored refresh token (Supabase→file→fresh mint), persists a new mint, and clears+re-mints on the 45-day wall (`RefreshTokenExpired`). Decouples the daily sync from Okta-session longevity AND from the operator's headless login (which MFA breaks) | ✅ offline `tests/test_sync_gating.py` (`test_membertools_uses_stored_refresh_token_no_mint`, `_mints_and_persists_when_no_stored_token`, `_expired_refresh_clears_and_remints`) + live DB round-trip of the `credentials` accessors (stake 503991) + migration applied to prod |
| E7 | **Member-Tools-primary resilience** (the golden-flow sync): the stake/ward STRUCTURE comes from the same `/api/v5/sync` payload (`context_from_sync`), so the daily sync completes with NO live LCR session (it dies within days; the 45-day token outlives it). `sync_stake` falls back to the Member Tools-derived `ctx_override` when the LCR `user_context` is dead instead of raising. Proven live: 503991 synced 93 members with a dead LCR session | ✅ offline `tests/test_sync_gating.py` (`test_context_from_sync_builds_stake_and_wards`, `test_sync_stake_falls_back_to_payload_context_when_lcr_dead`) |
| E8 | **Needs-reauth doesn't red-fail the daily workflow** (2026-06-12, ADR-011 "Ken" follow-up). A delegated stake with a dead LCR session AND no Member Tools token can't sync headlessly — it needs the LEADER to re-authorize (with a PASSWORD). That's an expected, leader-actionable state, not infra failure: `run_one_stake` classifies it (`_is_needs_reauth` — `login_required`/dead-session/no-credential signals), flags it via `_flag_needs_reauth` (credential marked stale → ops enrolled-stakes table + app banner + a once-per-streak leader email; `stakes.sync_state='needs_reauth'`), and exits 0. Real failures (LCR/Member Tools outage, a bug) are NOT re-auth cases and still red-fail. `_mint_and_sync`'s dead-session-no-token path raises a clearly-classifiable message | ✅ offline `tests/test_sync_gating.py` (`test_is_needs_reauth_true_for_reauth_signals`, `_false_for_real_failures`, `test_flag_needs_reauth_does_not_raise_without_db`) |
| E9 | **Birth date survives a dead LCR session — the missing-birthdays gap** (2026-06-12; reported on "Falls Lake"). Historically `birth_date` came ONLY from the LCR per-member profile fetch (`report._apply_profile`), which needs a live LCR session — so in the steady-state Member-Tools-primary sync (LCR session dead, 45-day token alive) it stayed `None` for the whole unit, silently breaking EVERY age gate (calling, priesthood, recommend, patriarchal, temple experiences). Fix: `membertools_adapter` now also reads `birth_date` from the bulk `/api/v5/sync` payload when present (tolerant `_birth_date`: flat `birthDate`/etc. or nested LCR `birth.{dateDisplay|date}`; `_birth_index` resolves it from the household roster as a fallback, like friend names). Stays `None` when the payload lacks it → the /mlt merge fills it as before (`_apply_profile` only fills `if not member.birth_date`), so no regression. Findings + the one live confirmation (run `tools/membertools_probe.py`, read the `birth_date` line) in `docs/DATA_GAP_BIRTHDATE.md` | ✅ offline `tools/test_suite.py::test_membertools_adapter` (flat birthDate on the member; household-resolved birth on the investigator — fails pre-fix: `birth_date` was always None) |
| E10 | **Ministering + calling survive a dead LCR session, and a structured field-gap report says WHY anything is still blank** (2026-06-13; reported "needs-profile-api leaking + ministering/friends blank"). The covenant-path PERSON record in `/api/v5/sync` carries NO ministering/calling/priesthood/recommend, so the adapter left them `NEEDS_PROFILE` for the /mlt profile merge — which never runs once the LCR session dies → the sentinel persisted and leaked to the UI. But the WHOLE payload DOES carry the unit-wide EQ/RS ministering org (`ministeringBrothers`/`ministeringSisters`) + the member directory (`households[].members[].positions`). Fix: `membertools_adapter` now rescues `ministering_brothers_sisters` / `ministering_assignment` / `calling` / `sex` from those top-level keys (`MinisteringIndex` ports the authoritative `SyncGoldenHourIndex` algorithm: EQ assigns per-household, RS per-individual, "gives ministering" = a companion; unit-coverage gives Yes/No/unknown — an UNKNOWN stays the sentinel so the merge preserves last-good, never a false "No"); minister + calling NAMES surface in `details`. `ministering_brothers_sisters` added to `db._GATED_COLUMNS` (+ `fake_db`) so its sentinel preserves last-good (it leaked raw before, being un-gated). `report.field_gap_report()` + `_emit_field_gap_report()` emit ONE structured log line + ONE `sync.field_gaps` observability event per stake (stashed on `stats['field_gaps']` → the diagnostics row) attributing each still-blank field to a reason ("not in bulk payload + profile merge skipped — re-auth", "calling lacks access", "not returned in the bulk payload this run") — so "ministering is blank" is answerable precisely, no per-member flood. temple_recommend / patriarchal / priesthood / endowment were thought GENUINELY-absent here, but a later live probe (E11) found priesthood/recommend/endowment ARE present after all (only patriarchal is truly absent). | ✅ `tests/test_membertools_adapter.py` (the upstream `SyncDirectoryAdaptersTests` fixture ported 1:1 — EQ/RS/multi-household/uncovered-unit/minister-names; `adapt_sync` rescue end-to-end; `field_gap_report` reason attribution — all fail pre-fix: ministering/calling were always `NEEDS_PROFILE`) + `backend/test_scenarios.py::scenario_membertools_ministering_rescue_and_sentinel` (rescue + uncovered-unit sentinel + `ministering_brothers_sisters` last-good preserved through a sentinel upsert — fails pre-fix: un-gated column clobbered to the raw sentinel) |
| E11 | **Priesthood office, temple recommend, and endowment survive a dead LCR session** (2026-06-13; live-probed vs stake 503991). E10 assumed these four were profile-only; a deep re-probe of `/api/v5/sync` (the FULL nested payload, not just the covenant-PERSON record — the same blind spot that hid birthday in E9) found three of the four ARE in the bulk payload: the member DIRECTORY carries `households[].members[].priesthood` (the OFFICE enum: DEACON/TEACHER/PRIEST/ELDER/HIGH_PRIEST/BISHOP/SEVENTY/…) and a per-member `ordinances[]` list (`type == ENDOWMENT`); the unit-wide `templeRecommendStatus[].recommends[]` roster carries `{memberUuid, status: ACTIVE/EXPIRED/ISSUED/CANCELED, type, expiration}`. Fix: `membertools_adapter` builds `_priesthood_office_index` / `_endowed_index` / `_recommend_index` / `_directory_uuids` and fills `aaronic_priesthood` / `melchizedek_priesthood` (via `report.priesthood_from_office`) / `living_ordinance` / `temple_recommend` — applying the SAME eligibility gating as `report._apply_profile` (Aaronic = male & turning-12; Melchizedek = male & 18+ now & member-1yr; an ineligible endowment "No"→N/A; recommend mapped to Active/Expired/No like `member_profile._recommend_label`). A member ABSENT from the directory/roster (e.g. an investigator) keeps the `NEEDS_PROFILE` sentinel → the merge/last-good preserves (these columns were already in `db._GATED_COLUMNS`, so no DB change). **Patriarchal blessing is the ONLY field genuinely absent** from the whole payload (no `hasPatriarchalBlessing` flag, no PATRIARCHAL ordinance type anywhere) → it alone stays profile-only for the /mlt merge. Live fill-rates on 503991 (93 covenant rows): priesthood 26 Yes / 13 No / 37 N/A, recommend 36 Active / 13 Expired / 27 No, endowment plausible spread, patriarchal 93 sentinel; the 17 sentinels per rescued field = the 17 investigators (not in the directory). | ✅ `tests/test_membertools_adapter.py` (`_priesthood_office_index` / `_endowed_index` / `_recommend_index` index builders; `test_adapt_sync_rescues_priesthood_endowment_recommend` — endowed-elder/young-deacon/adult-woman/ward-200 eligibility gates + strongest-recommend-label-wins; `test_adapt_sync_no_directory_keeps_priesthood_recommend_sentinels`; updated `field_gap_report` reasons — all fail pre-fix: these four were always `NEEDS_PROFILE`) + `backend/test_scenarios.py::scenario_membertools_priesthood_recommend_endowment_rescue` (rescue + not-in-directory sentinel + `temple_recommend` last-good preserved through a sentinel upsert) · probe tool `tools/membertools_deep_probe.py` (read-only, PII-safe — structure/counts only) |
| E12 | **CI diagnostics hygiene — the probe doesn't red-fail, the live-smoke isn't stake-shape-fragile, and the endpoint-health trend de-dupes legacy id rows** (2026-06-13). Three scheduled-workflow failures with one theme (expected/known states masquerading as failures): (a) **`probe-lcr` failed every 4h** — `backend.probe` does a FRESH `okta_login.login()`, but the operator account now has MFA, so Okta returns `select-authenticator-authenticate` and the headless login can't finish (the daily sync is unaffected — it runs off the stored 45-day Member Tools token, [[project_golden_sync_flow]]). Fix: the probe catches the auth failure, logs it, and exits 0 (same posture as E8's needs-reauth), so a chronic, leader-/MFA-bound condition is a skip, not a red X. (b) **`live-smoke`/`test_power_users` newly failed** once a SECOND stake (Springville) enrolled alongside Raleigh in prod: the suite picked a stake_leader with an unscoped `limit 1`, grabbed a DIFFERENT stake's leader, and the unit-scoped `invite_power_user(email, unit)` then correctly raised "not authorized to grant access to that unit". Fix: scope the pick to the stake under test (`stake_id = 503991`), degrade cleanly when none. (c) the admin **endpoint-health trend** still listed half-normalized `/api/report/one-work/details/{id}<hextail>` rows from the PRE-FIX metrics normalizer as distinct "hot" endpoints (the "View all" list renders the broker rows raw); `admin._norm_endpoint` now collapses any `{id}…` segment to `{id}` on read (matching `apps/web AdminPage.normEndpoint`), merging them into the one route key with no data migration (and they age out of the 14-day window). | ✅ offline `tools/test_suite.py::test_endpoint_normalizer` (ids + legacy `{id}<hex>` collapse; fails pre-fix) + live smoke `test_power_users` (8/8 PASS vs prod's two-stake data, incl. "stake grants ward-only scope"; fails pre-fix on the cross-stake pick) · probe skip is CI-env-conditional (operator MFA): verified by the green `probe-lcr` run |

| E13 | **A ward-scoped credential can NEVER sync (or overwrite) a stake — the Green Level Ward incident** (2026-06-13). A Ward Mission Leader signed in WITH sync consent; enrollment keyed the credential by *their* unit number, creating a phantom "Green Level Ward" stake — and that ward leader's Member Tools token returns the PARENT stake as its org root (`units[0]`) while exposing covenant-path data for only their ward, so the daily sync's `ctx_override` resolved to the parent stake and upserted the ward's 7 members onto the whole stake, reconciling the other 86 away (Raleigh 93 → 7, all "Green Level Ward"; the stake president was forced into a ward view). Three-layer fix: (a) **`sync_stake` scope guard** — `expected_stake_unit` (the credential's registered unit) is threaded through; if it's a CHILD of the Member Tools org root it raises `CredentialScopeMismatch` BEFORE any write, and `run_one_stake`/`run_delegated` revoke the credential + flag it (no red-fail) — auto-healing any legacy ward credential; (b) **enroll ward-gate** — `evaluate_and_maybe_store` sets `can_enroll=False` and refuses to store for a WARD/BRANCH-level session (no child units + not `can_pull_all`), returning a friendly `ward_scoped`/`enroll_block_reason` (a ward leader sees their unit via their provisioned `ward_leader` RLS role — no enrollment); (c) **DB backstop** — migration `0052` makes `enroll_stake_credential` RAISE when `p_unit_number` is a known WARD/BRANCH of an existing stake. Also adds R3 revoke-risk + R4 downgrade warnings (`would_downgrade`/`existing_provider`) to the enroll response, surfaced in Sync settings on all surfaces. | ✅ offline `tests/test_sync_gating.py` E9 (`test_ward_credential_refuses_to_sync_a_stake`, `test_stake_credential_passes_the_scope_guard`, `test_scope_guard_inert_without_expected_unit` — fail pre-fix: the `expected_stake_unit` param didn't exist) + live DB verification of the `0052` guard (ward unit 1102966 BLOCKED, stake 503991 + a brand-new unit ALLOWED, all rolled back) + prod cleanup (phantom stake deleted, Raleigh re-synced 93/93) |
| E14 | **Temple experiences survive a dead LCR session — the last bulk-rescuable fields** (2026-06-13; completes the E9–E11 lineage). `family_name_prepared` / `first_temple_visit` were the only covenant-path fields still sourced ONLY from the one-work/profile path (`report.parse_temple_experiences` reading `newMemberOtherCommitments` → `{name, isKeeping}`), so `adapt_person` left them `NEEDS_PROFILE` and they never filled once the LCR session died (the Green Level Ward restore exposed it: 7/93 filled). The bulk `/api/v5/sync` payload DOES carry them — per covenant-PERSON `otherCommitments` → `{title, interested}` ("Prepare a Family Name for the Temple" / "Perform Baptisms for Deceased Ancestors"). Fix: `parse_temple_experiences` now reads BOTH shapes (one source of truth), and `adapt_person` extracts the two fields with the SAME age gate as `_apply_profile` (ineligible "No" — not yet turning 12 — → N/A; absent list → sentinel for the merge). Calibrated against 5 known-good survivors (`interested:true`→Yes / `false`→No matched stored truth exactly). **`patriarchal_blessing` remains the ONE field genuinely absent from the whole bulk payload** — it alone needs a live-session /mlt profile fetch (a one-time re-auth). Live fill-rate on 503991 after the fix: 7 → 91/93 (the 2 sentinels = members with no `otherCommitments` list). | ✅ offline `tests/test_membertools_adapter.py::test_adapt_sync_rescues_temple_experiences_from_other_commitments` (adult Yes/No, under-12 "No"→N/A age gate, absent-list→sentinel — fails pre-fix: both fields were always `NEEDS_PROFILE`) + `test_adapt_sync_no_directory_keeps_sentinels` extended + live re-adapt of 503991 (7 → 91/93; DB confirmed) |

| E16 | **The 45-day sync token survives its full window — closing the early-expiry gaps (#0, 2026-06-14).** Diagnosed live on stake 503991: its earlier enrolls (Jun 9–12) persisted **NO** Member Tools 45-day token (the broker's psycopg2 bootstrap failed, and lanes with no live SSO session couldn't mint), so the daily sync fell back to the warm Okta session — which dies within days → the "Sync stopped, your Church session expired" banner appeared **well before 45 days** (the token only first stored Jun 12 21:26). The stored-token-first refresh / clear-on-wall / mint-bootstrap / loud-ERROR logging already existed (E6); four remaining gaps are now closed so a persistence-or-refresh slip can't silently strand a stake: **(F2) rotation-safe refresh** — `_membertools_access_token` captures the full `refresh()` result and re-persists a ROTATED refresh token (the original `membertools_minted_at` 45-day clock preserved by `save_membertools_refresh`'s `coalesce`), so a future Okta refresh-token rotation can't kill the next day's renew; **(F3) the ~day-40 re-auth nudge keys off the REAL token clock** — `claim_age_notification` ages on `coalesce(membertools_minted_at, updated_at)` because the daily refresh renews the bearer WITHOUT rewriting the credential blob, so `updated_at` is NOT the token's clock (byte-identical to before when no token is stored → no B8 regression); **(F4) verify-after-persist** — `persist_membertools_refresh_rest` reads the column back and raises if the PATCH matched no row (a 200/204 no-op from RLS or a missing credential row no longer silently vanishes, which the daily sync would have surfaced as needs-reauth days later); **(F5) tri-state token read** — `_membertools_token` raises `_TokenStoreUnavailable` on a DB-read FAILURE (vs. silently falling to an empty CI file store) so a transient DB hiccup can't masquerade as "no token" and trigger a false re-mint / needs-reauth flag. | ✅ offline `tests/test_sync_gating.py` (`test_membertools_rotation_repersists_new_refresh_token`, `_same_refresh_token_not_repersisted`, `_token_store_db_error_raises_not_empty`, `_token_db_error_falls_back_to_file_store` — F2/F5, fail pre-fix) + `backend/test_broker.py::test_membertools_persist_verifies_after_patch` (F4 — empty verify-GET raises; fails pre-fix) + `tests/test_credential_and_email_gates.py::test_age_alert_uses_membertools_token_clock` (F3 MT-clock aging, test-project DB — fails pre-fix: it keyed off `updated_at`) · live diagnosis via `tools/render_logs.py` + read-only credential-state query (all 3 prod stakes now hold a stored token) |
| E15 | **The needs-profile-api sentinel never reaches a client** (2026-06-13, item 2b; reported "needs-profile-api for OTHER fields is unacceptable"). The gated-merge `coalesce(nullif(excluded.c, sentinel), members.c)` only runs ON CONFLICT (UPDATE); on a member's FIRST INSERT no conflict fires, so the raw sentinel landed verbatim and stuck forever (`coalesce(NULL,sentinel)=sentinel` thereafter) — leaked across ALL stakes (baptism_date=32, calling=68, recommend=30, …). `db._scrub_sentinel` maps a gated column's sentinel → NULL at the write boundary (INSERT writes NULL; UPDATE still preserves last-good; `field_meta` reads the original so staleness `t`/`f` is unchanged) → clients get NULL (rendered ⚠, D22). Also `baptism_date` for BIC children (covenantPathMembers stub, no `confirmationDate`) now falls back to `_baptism_date_index` (directory CONFIRMATION→BAPTISM; identical to the day for 74/74 carrying both; the 2 Lambert kids → 2024-12-20). | ✅ `tests/test_membertools_adapter.py` (baptism directory rescue) + `tools/test_suite.py::test_db_sentinel_scrub_on_insert` (end-to-end via real `upsert_members`) — both fail pre-fix; test_suite 26/26, adapter 23/23, F821 clean |
| E17 | **Member + missionary photos via the Member Tools token — no LCR session (#11).** Photos finally work without a live LCR session or `cmisId`: `POST /api/v5/sync/files` with `Accept: application/zip` returns a ZIP of WebP avatars keyed by UUID (`MEMBERS_PHOTOS/<uuid>.webp`, `MISSIONARIES_ASSIGNED_PHOTOS/<uuid>.webp`, `MISSIONARIES_SERVING_PHOTOS/<unit>/<uuid>.webp`; recipe from rickybloomfield/Mission-KPIs — my earlier probe 406'd only on `Accept: json`). `lcr_client.membertools.fetch_sync_files()` pulls the bundle (members + missionaries kept, households/temples excluded); `backend.photos.sync_photos_from_bundle()` unzips, downsizes each to a JPEG thumbnail, uploads to the private `member-photos` bucket, signs, and sets `members.photo_url` — returning a `{missionary_uuid: signed_url}` map for the roster. Wired into the daily sync's `--photos` pass off the same Member Tools bearer (`args._mt_access_token`); the old LCR-avatar `sync_photos_for_stake` stays as a fallback. ~50MB/stake → run periodically. | ✅ validated LIVE on stake 503991 (16 member + 36 missionary photos uploaded, 0 skipped; `members.photo_url` populated) + ruff F821 + import check · ⚠️ enable the `--photos` workflow var to run it in CI |
| E18 | **The `--photos` pass never red-fails the sync, and avatars actually run in CI** (2026-06-18; completes E17). The daily-sync job (`.github/workflows/daily-sync.yml`) set `SUPABASE_URL` but NOT `SUPABASE_SERVICE_ROLE_KEY` — which `backend/photos.py` needs for the REST Storage API — so every scheduled `--photos` run hit `_sb()` and, because it raised **`SystemExit`** (a `BaseException`, NOT caught by `daily_sync`'s `except Exception` "never fail the data sync over avatars" guard), the WHOLE sync exited code 1 AFTER the data had already upserted (the "synced 101 fine, then errored" report). And because the photo pass died on its first line, missionary avatars (`sync_photos_from_bundle` → `attach_missionary_photos`) never ran → missionary pictures never appeared (member rows still showed initials). Fix: (a) `_sb()` raises a catchable `RuntimeError` instead of `SystemExit` so the best-effort guard holds; (b) add `SUPABASE_SERVICE_ROLE_KEY` to the sync job env so photos actually run. | ✅ `tests/test_photos.py` (`_sb()` missing-env raises a catchable Exception, NEVER SystemExit — fails pre-fix: it raised SystemExit) + ruff F821 · workflow secret added (config) · ▶ confirm on the next scheduled run that member + missionary `photo_url` populate |
| E19 | **Loud alarm for a DEGRADED /api/v5/sync payload** (2026-06-18 review). A bulk-RESCUABLE field (temple_recommend, living_ordinance, calling, …) showing 100% missing across a populated stake (≥20 members) is the signature of a PARTIAL payload — a dropped household directory / templeRecommendStatus sub-tree — which silently degraded the whole stake to sentinels with only a soft per-field line (the 503991 `temple_recommend=101/101 + living_ordinance=101/101` case). `report._emit_field_gap_report` now emits a distinct `logger.error("DEGRADED /api/v5/sync …")` + a `sync.degraded_payload` observability event + a `degraded_payload_fields` stats key (on the diagnostics row), distinct from a dead LCR session (which only blocks PROFILE-only fields like patriarchal). Read-only over the already-computed counts — the sentinel / preserve-last-good behavior is untouched (never writes a false value). | ✅ `tests/test_field_gaps.py` (100%-missing rescuable field flagged + error log; partial / small-stake NOT flagged; patriarchal-only gap is NOT a degraded payload — fails pre-fix: no degraded signal) + ruff F821 |
| E20 | **Leadership enrich no longer cries "stale action id" on a dead session** (2026-06-18 review). An empty positionType harvest warned "the 'leadership' action id may be stale; re-capture" for BOTH a genuinely stale id AND a dead delegated LCR session (the POST is 401'd / redirected) — a false alarm that fired TWICE per run on stake 503991, sending operators to re-capture a config that wasn't broken. `enrich_role_names` now inspects the response: a non-200 (dead session) logs at DEBUG ("session not live"); only a real 200 with no positionType still warns stale. `fetch_leadership`'s public signature is unchanged (`backend/roles.py` unaffected). | ✅ `tests/test_leadership_enrich.py` (302 dead-session → no stale warning, DEBUG instead; 200-no-positionType → stale warning still fires — fails pre-fix: always warned) + ruff F821 |
| E21 | **A no-calling directory member syncs as a real "No" — Member Tools omits `positions`, never sends `[]`** (2026-07-06; reported "Calling unavailable — why?"). `calling` is bulk-RESCUED from the directory's `positions` (E10), but the adapter's `_calling_index` required the KEY to be present — and a live probe of the 503991 payload showed the API **omits the key entirely** for a member with no calling (2987 directory members; exactly the 896 with a calling carry the key, all non-empty). So every no-calling member stayed "unknown" → NEEDS_PROFILE → NULL → a permanent ⚠ in the Calling column (59/76 Raleigh members; the /mlt merge that would have corrected it never runs once the LCR session dies). Fix: every directory member gets an index entry (key absent → `[]` = a real "No"); someone absent from the directory entirely still stays out (genuinely unknown → sentinel → merge/last-good). Guard: a payload where NO member carries `positions` (a token whose scope can't see callings at all) returns `{}` — never a fabricated stake-wide "No". | ✅ `tests/test_membertools_adapter.py::test_calling_index_key_omitted_is_real_no` + `test_calling_index_no_positions_anywhere_stays_unknown` + `test_adapt_sync_calling_no_for_key_omitted_directory_member` (all fail pre-fix: key-omitted stayed unknown) + live probe of the 503991 payload (counts above) |
| E22 | **On-demand profile fill: `POST /auth/profile-refresh` + progress `GET /auth/profile-refresh/status`** (2026-07-06; reported "provide endpoint to fill and report back"). Patriarchal blessing (and any field a member missed in the bulk directory) only fills from a live-session `/mlt` read — which for a self/CI-restored credential NEVER ran (the profile worker only spawns from the broker's enroll path; Raleigh sat at 0/76 patriarchal). New broker lane, gated on a signed-in **stake leader** of the stake + a non-revoked credential whose calling has `menu.view.member.profiles`: POST tries the STORED delegated session first (fast probe = GET the first gap member's `/mlt` profile page; dead → `needs_reauth` so the client opens the ReauthDialog, whose enroll path spawns the same worker off the fresh session); live → spawns `_refresh_profile_worker` (`source=on_demand`). The worker now publishes live progress (`_REFRESH_PROGRESS` → the status route: state/total/done/filled), stamps `field_meta` `f`/`t` for exactly the fields it fills (freshness views stay truthful), and records the outcome to `sync_diagnostics` (`kind='profile_refresh'`; INSERT grant = migration 0057). Status also returns per-field missing counts (investigators excluded) so the fill-data sheet shows precisely what's missing before/after. | ✅ `backend/test_broker.py::test_profile_refresh_worker_fills_via_live_session` (extended: field_meta stamps + progress + diagnostics row) + `test_profile_refresh_on_demand_start_and_status` (dead session → needs_reauth; live → started/on_demand; already-running not restarted; status missing-counts + last_refresh — fails pre-fix: routes/functions absent) + migration 0057 applied |
| E23 | **LCR redeploy rotates ALL `/mlt` action ids → HTTP discovery self-heals off the live session** (2026-07-19; reported "re-authenticated but still miss callings and patriarchal"). The 2026-07 Turbopack rebuild rotated every server-action id AND renamed the actions (record→`getMemberData`, recommend→`getRecommendData`, ministering→`getMinisteringData`, callings→`getCallingsAndClassesData`, leadership→`getUnitOrgWrapper`, missionary→`getFullTimeMissionaryWrapper`) — every profile POST 404'd, and the only self-heal was a **Playwright** run that can't work where it matters (no playwright on Render — `No module named 'playwright'` in the Jul-16 re-auth log — and a headless operator login is MFA-blocked anyway), so the re-auth worker filled 0/79 and gap-fills were dead for weeks (`resolved: []` heals since ≤Jun-27). Fix: `action_discovery.discover_http` — GET the profile+orgs pages with the caller's LIVE session, walk the chunk graph (script srcs + `/`-escaped inline flight refs — missing those is how a harvest goes blind), read the `createServerReference("<id>",…,"<name>")` registrations (the name survives minification), map by name (legacy-name fallbacks), VERIFY the record canary with a real POST before persisting; a renamed-everything future build falls back to shape-probing every harvested id against the DETECTORS; a dead session raises (never persists garbage). `heal()` prefers HTTP, Playwright is last-ditch; DEFAULTS reseeded with the six verified ids. Response shapes confirmed unchanged (record/recommend/ministering/callings parse as-is). | ✅ `tests/test_action_discovery.py` (8: minified-registration parse, escaped-chunk-ref harvest, name→target map incl. legacy fallback, full discover_http with fake session — name-map + single canary verify, verify-miss discards, renamed-build shape-probe fallback, dead-session raise, heal persists `method="http"`) + live: discovery off the stored Raleigh cookies mapped+verified all six ids; worker then filled **79/79 Raleigh + 28/28 Springville** members (calling/patriarchal/recommend/ministering NULLs → 0) |

### F. Emails (non-spammy invariants)

| # | Scenario | Status |
|---|---|---|
| F1 | Enroll confirmation: once per enroll, best-effort | ✅ e2e-mock (notify stubbed; assert single call) |
| F2 | Stale-credential alert exactly once per failure streak (atomic claim under CONCURRENT syncs; success re-arms the edge) | ✅ `tests/test_credential_and_email_gates.py` (test-project DB) |
| F3 | Invitations: emailed-once, revoked skipped, daily cap, failed send stays pending for retry | ✅ `tests/test_credential_and_email_gates.py` (test-project DB) |
| F4 | Digest once per leader per run + never for an empty scope; handoff fires once per (person, phase), unsent transitions not burned; weekly respects the owner-preview gate | ✅ `tests/test_digest_and_handoff_gates.py` (test-project DB) |
| F5 | Admin-invite approval: token-gated, single email, expiry | ⬜ TODO |

### G. Onboarding / offboarding a stake

| # | Scenario | Status |
|---|---|---|
| G1 | New-stake first enroll: stake row created, kickoff scoped, "setting up your stake" state, roles provisioned by first sync | ✅ e2e-mock (enroll+kickoff stubs) · 🔶 matrix (post-sync visibility) |
| G2 | Offboard tiers: wipe (members gone + freshness reset; roles + credential survive) → remove (credential/members/roles/diagnostics/stake erased, admin-gated 403 for others, idempotent) | ✅ `tests/e2e/test_deferred_report_offboard.py` + offline wipe test |
| G3 | Power users of an offboarded stake see empty app, never errors | 🔶 matrix |

## When something breaks in prod

Write the post-mortem scenario into this catalog **first**, then the regression test, then the
fix (the test must FAIL against the broken code — see `apps/web/src/test/modal.test.tsx` and
`test_broker.py::test_full_eval_enroll_reaches_scrape_and_stores` for the pattern).
